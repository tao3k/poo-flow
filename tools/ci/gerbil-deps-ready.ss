#!/usr/bin/env gxi
;;; Verify the exact dependency surface required by the native build owner.

(import :clan/base
        :clan/poo/object
        :gslph/src/building/facade)

(displayln "gerbil-package-cache-ready")
