[features]

hooks = true
unified_exec = true

[marketplaces.asp-project]
source_type = "local"
source = "."
[plugins."asp-codex-plugin@asp-project"]
enabled = true

[agents.asp_explorer]
description = "Read-only ASP search explorer for hook-safe evidence collection."
config_file = "agents/asp-explorer.toml"
nickname_candidates = ["ASP owner", "ASP rg", "ASP selector", "ASP search"]
