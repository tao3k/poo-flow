#+TITLE: Agent Semantic Protocols
#+PROPERTY: ASP_CONTRACT_VERSION 1
#+PROPERTY: ASP_TEMPLATE_KIND skill
#+PROPERTY: ASP_RENDERER rust

#+CONTRACT_ORG: ./SKILL.contract.org#asp.skill.v1

* Contract Metadata
:PROPERTIES:
:ASP_SECTION: metadata
:ASP_RENDER: static
:END:

Source template for generated ASP skill contracts. Keep the static body focused
on agent search flow; generated sections are owned by Rust.

# BEGIN_ASP_GENERATED notice
#+begin_quote
IMPORTANT: Generated from the repository root =SKILL.org= template, validated by =SKILL.contract.org=, and expanded from the current provider activation. Do not edit this installed copy. Edit root =SKILL.org=, =SKILL.contract.org=, or =.agents/asp.toml=, then rerun =asp install plugin --codex .= for Codex or =asp install hook --client claude .= for Claude.
#+end_quote
# END_ASP_GENERATED notice

* ASP Org Reference
:PROPERTIES:
:ASP_SECTION: asp-org-reference
:ASP_RENDER: generated
:ASP_SOURCE: project_state_paths.protocol_home/org/skills/ASP_ORG.org
:END:

# BEGIN_ASP_GENERATED asp-org-reference
The ASP Org operational skill is materialized in project state, not inside the Codex plugin directory.
Use this reference when durable Org planning, contracts, templates, or flow state are needed.
Resolve project-local state paths through =asp paths= before opening Org artifacts from a different working directory.

| Field | Value |
|-------+-------|
| Project root | =asp paths --get projectRoot= |
| REFER_ORG | =.cache/agent-semantic-protocol/org/skills/ASP_ORG.org#asp-org= |
| ASP Org skill | =asp paths --get orgStateSkill= |
| Org artifacts | =asp paths --get orgArtifacts= |
| Project-relative ASP Org skill | =.cache/agent-semantic-protocol/org/skills/ASP_ORG.org= |
| Project-relative Org artifacts | =.cache/agent-semantic-protocol/artifacts/org= |
| Missing state | Run =asp org capture init= from the project root. |
# END_ASP_GENERATED asp-org-reference

* Runtime Activation
:PROPERTIES:
:ASP_SECTION: activation
:ASP_RENDER: generated
:ASP_SOURCE: .agents/asp.toml,path,.bin
:END:

# BEGIN_ASP_GENERATED activation
Generated from =.agents/asp.toml=, =PATH=, and project =.bin= provider binaries.

| Field | Value |
|-------+-------|
| Runtime | =asp= |
| Version | =0.1.0= |
| Active provider count | 6 |
| Active language list | See generated =Provider Contracts= subtrees. |
# END_ASP_GENERATED activation

* Agent Operating Contract
:PROPERTIES:
:ASP_SECTION: operating-contract
:ASP_RENDER: static
:END:

** Search Flow
Search is a staged query language. Start at the earliest stage that current
evidence justifies, then follow =recommendedNext= or =nextCommand=.

| Stage | Command | What the agent learns |
| capability | =asp <language> guide --workspace .= | Supported surfaces, axes, recipes, and dependency profiles. |
| topology | =asp <language> search prime --workspace . --view seeds= | Workspace owners, provider roots, package/member hints, and first action frontier. |
| query refinement | =asp <language> search pipe '<question>' --workspace . --view seeds= | Ranked evidence, query terms, missing facts, risk, and next command. |
| owner expansion | =asp <language> search owner <owner> items --query '<term>' --workspace . --view seeds= | Owner-local symbols, tests, imports, docs-use, and exact selectors. |
| finder recovery | =asp fd -query '<path-or-owner-term>' --workspace <scope>= | Candidate owners when path or selector confidence is low. |
| indexed text recovery | =asp rg -query '<term>' --workspace <scope>= | Candidate owners from indexed text when a search packet asks for scoped recovery. |
| exact projection | =asp <language> query --selector <selector> --workspace . --code= | Source text for a precise selector already emitted by search. |

** Execution Fan-out
Subagents are optional client execution. They consume search-flow axes; they do
not define new query semantics.

| Trigger | Action |
| session search worker id | Open at most one ASP search worker per main session, record its id in parent state, and reuse that id for later search fan-out. |
| independent =actionFrontier= or =targetActions= rows | Assign each bounded command group to a read-only worker. |
| worker result | Return compact evidence, missing facts, risk, and next action to the parent. |
| no subagent support | Run the same command groups sequentially in the parent. |

** Dependency Search Topology
Dependency questions stay inside provider search until the provider reports a
missing dependency frontier.

| Command | Purpose |
| =asp <language> search dependency <name> --workspace . --view seeds= | Return manifest owner, dependency edge, import owners, docs/public API use, source/runtime hints, tests, and next command. |
| =asp <language> search reasoning query-deps --workspace . --view seeds= | Use the shared dependency-topology profile when the provider advertises it. |
| =asp <language> search owner <manifest-owner> items --query '<dependency>' --workspace . --view seeds= | Inspect dependency evidence in the manifest or package owner. |

** Document Search
Document providers expose AST facts rather than policy harnesses.
Use asp org capture <contract-kind> for non-mutating Org entry plans backed by contracts such as plan, sdd, adr, bdd, prd, or execplan.

| Command | Purpose |
| =asp org guide --workspace .= | Show Org element kinds, fields, capture, and contract trace commands. |
| =asp org query --kind task --field todo=TODO --workspace . --view metadata= | Find TODO headlines as task facts. |
| =asp org query --kind checklistItem --field checked=false --workspace . --view metadata= | Find unchecked checklist items without collapsing them into tasks. |
| =asp org query --selector <structural-selector> --workspace . --content= | Print matched element content after a structural selector is known. |
| =asp org capture <contract-kind> --kind task --title <TITLE> --target-file <file.org>= | Render a non-mutating Org entry plan for review before writing. |
| =asp md guide --workspace .= | Show Markdown document query/search surfaces. |

** Failure And Recovery

| Command | Purpose |
| =asp <language> search failure '<compact-failure>' --workspace . --view seeds= | Project failure text into candidate owners, hot blocks, query profiles, and next actions. |
| =asp <language> search guide --workspace .= | Confirm a failure or reasoning profile exists before reconstructing a command. |

** Provider Contract Rules
- Provider =guide= output is the command contract; do not infer unsupported axes.
- Dependency topology must include manifest, dependency edge, import owner,
  public API/docs use, test witness, and next action when the provider can
  prove them.
- Search output should be compact evidence, not source dumps.
- Treat =missing=, =evidenceGrade=unknown=, and =witness pending= as real
  frontier gaps.
- Keep language project parsing in the language provider. ASP routes and
  indexes; it does not reimplement package membership or source/test scope.

* Provider Contracts
:PROPERTIES:
:ASP_SECTION: providers
:ASP_RENDER: generated
:ASP_SOURCE: HookActivation.providers
:END:

# BEGIN_ASP_GENERATED providers
Detected from provider binaries plus =.agents/asp.toml=; only activated languages are listed.

** rust
:PROPERTIES:
:LANGUAGE_ID: rust
:PROVIDER_ID: rs-harness
:BINARY: rs-harness
:EXECUTION: external-process
:FACADE: asp rust
:COMMAND: rs-harness
:DOCUMENT_PROVIDER: false
:ENABLED: true
:END:

Use =asp rust guide .= before rust-specific exploration.
Use =asp rust search prime --workspace <workspace-root> --view seeds= only when the rust owner map is unknown; exact selectors, owners, symbols, dependencies, or hook frontiers should go straight to the provider-owned query, owner, finder, guide, or dependency route.

** typescript
:PROPERTIES:
:LANGUAGE_ID: typescript
:PROVIDER_ID: ts-harness
:BINARY: ts-harness
:EXECUTION: external-process
:FACADE: asp typescript
:COMMAND: ts-harness
:DOCUMENT_PROVIDER: false
:ENABLED: true
:END:

Use =asp typescript guide .= before typescript-specific exploration.
Use =asp typescript search prime --workspace <workspace-root> --view seeds= only when the typescript owner map is unknown; exact selectors, owners, symbols, dependencies, or hook frontiers should go straight to the provider-owned query, owner, finder, guide, or dependency route.

** julia
:PROPERTIES:
:LANGUAGE_ID: julia
:PROVIDER_ID: julia-lang-project-harness
:BINARY: asp-julia-harness
:EXECUTION: external-process
:FACADE: asp julia
:COMMAND: asp-julia-harness
:DOCUMENT_PROVIDER: false
:ENABLED: true
:END:

Use =asp julia guide .= before julia-specific exploration.
Use =asp julia search prime --workspace <workspace-root> --view seeds= only when the julia owner map is unknown; exact selectors, owners, symbols, dependencies, or hook frontiers should go straight to the provider-owned query, owner, finder, guide, or dependency route.

** gerbil-scheme
:PROPERTIES:
:LANGUAGE_ID: gerbil-scheme
:PROVIDER_ID: gerbil-scheme-harness
:BINARY: gslph
:EXECUTION: external-process
:FACADE: asp gerbil-scheme
:COMMAND: gslph
:DOCUMENT_PROVIDER: false
:ENABLED: true
:END:

Use =asp gerbil-scheme guide .= before gerbil-scheme-specific exploration.
Use =asp gerbil-scheme search prime --workspace <workspace-root> --view seeds= only when the gerbil-scheme owner map is unknown; exact selectors, owners, symbols, dependencies, or hook frontiers should go straight to the provider-owned query, owner, finder, guide, or dependency route.

** org
:PROPERTIES:
:LANGUAGE_ID: org
:PROVIDER_ID: orgize
:BINARY: asp
:EXECUTION: embedded
:FACADE: asp org
:COMMAND: asp
:DOCUMENT_PROVIDER: true
:ENABLED: true
:END:

Use =asp org guide .= before document element navigation.
Use =asp org query= for parser-owned document elements and metadata.

** md
:PROPERTIES:
:LANGUAGE_ID: md
:PROVIDER_ID: orgize
:BINARY: asp
:EXECUTION: embedded
:FACADE: asp md
:COMMAND: asp
:DOCUMENT_PROVIDER: true
:ENABLED: true
:END:

Use =asp md guide .= before document element navigation.
Use =asp md query= for parser-owned document elements and metadata.
# END_ASP_GENERATED providers

* Client Surface
:PROPERTIES:
:ASP_SECTION: client-surface
:ASP_RENDER: static
:ASP_CLIENT: codex
:END:

ASP is a CLI contract. Client lifecycle and installation details belong outside
this skill body.
