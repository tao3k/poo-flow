;;; -*- Gerbil -*-
;;; Boundary: upstream kernel profile data for built-in POO Flow modules.
;;; Invariant: importing the kernel never loads presentation or doctor owners.

(import "core.ss")

(export (import: "core.ss"))
