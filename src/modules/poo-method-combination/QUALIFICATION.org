#+title: POO method combination local qualification — 2026-09-08

* Scope
Historical pre-promotion evidence for the now first-class local module, single
receiver, native instance/type owner lookup. No upstream changes, production
telemetry activation, complete CLOS class system, or full-package acceptance is
claimed. Path-sensitive qualification must be rerun after the module promotion;
the receipts below remain historical evidence rather than current acceptance.

* Current source receipt
Command exited 0 after the retained-frame and reusable-consumer additions:
#+begin_src sh
./.devenv/devenv-profile-exec gxtest t/poo-method-combination-test.ss t/poo-method-combination-contract-test.ss t/poo-method-combination-next-test.ss t/poo-method-combination-observation-test.ss t/poo-method-combination-performance-test.ss t/observability-framework-test.ss t/gerbil-poo-debug-admission-test.ss
#+end_src

| Suite | Assertions |
|-------+------------|
| standard combination | 21 |
| native contracts | 51 |
| effective invocation | 23 |
| real Observation scenario | 15 |
| measured cache scenario | 7 |
| existing Observability framework | 53 |
| existing upstream debug admission | 33 |
| total | 203 |

Representative values, not just totals:
- Diamond primary order =D B C A=; after order =A C B D=; shared A executes once.
- Repeated around next with replacement/original arguments returns =(1 7)=;
  auxiliary trace is =((before 1) (after 1) (before 7) (after 7))=.
- Primary replacement returns =(replacement 42)= while its core's before/after
  retain =((original 7) (original 7))=.
- Real rejected Module observation retains =(imports)= / =prototype-mismatch=.
- A retained lexical frame replays replacement =9= and then original =7, 7=.
- One opt-in detailed renderer handles rejected/accepted/rejected observations
  with decisions =(#f #t #f)= and failure counts =(1 0 1)=, reusing its plan but
  returning fresh summaries and producing no output. Default summaries are unchanged.
- 1000 warm calls with 8 delegating layers plus one base primary yield checksum
  =507500=, matching the native baseline, and reuse the identical plan object.

* Historical AOT receipts before the Scheme-native migration
Current Scheme-native reproduction command (the shell prototype was removed):
#+begin_src sh
./.devenv/devenv-profile-exec gxi t/qualification/poo-method-combination/qualify.ss
#+end_src

The following 79-assertion records describe the earlier limited gate, not the
expanded seven-suite Scheme-native qualification. Interrupted migration runs
and module-load checks do not supersede that historical gate.

The first raw-profile attempt failed during C compilation with =FILE=/stdio
errors caused by mixing the Nix SDK with Homebrew Gerbil. It is a failed gate,
not a Scheme semantic failure. The script now mirrors =devenv.nix='s native
Darwin child-process boundary; no global environment or compiler installation
was changed.

Before the frame optimization, all four owned modules and four scenario/test
modules compiled, and the compiled three-suite run passed 79 assertions.
Runtime =load-module= resolution independently confirmed the exact four new
=.o1= paths under the temporary =poo-combination-aot.hNWiKH/lib= directory.
The optimized fresh-library qualification also exited 0 with 79 compiled
assertions (21 combination, 51 contracts, 7 performance). All eight requested
modules produced nonempty =.ssi= and =.o1= files. The source fingerprint remained
unchanged across compilation. Actual native module resolution was checked under:

#+begin_src text
/private/var/folders/pj/n33bqzy94yn9wxggj3rny0d00000gn/T/poo-combination-aot.CDmU4t/lib
#+end_src

Toolchain: =Gerbil v0.18.2 on Gambit v4.9.7-6-g64f4d369=, =gxc -O=.
The runtime-path check also exited 0 for =types~0=, =objects~0= and =func~0=,
verifying implementation units in addition to the four main loader modules.
The 79 compiled assertions overlap the source receipt and are not extra cases.
The 18 invocation and 7 Observation assertions are included in the source run,
not in the three-suite compiled runner. The temporary artifact directory is a
local evidence location, not an installation or a durable release artifact.

* Performance evidence
Same benchmark input: 1000 calls, depth 8, one base primary plus 8 delegated
primaries, exact checksum =507500=. Times are milliseconds and observations,
not a production SLA or universal benchmark.

| Phase | Before, ms | Static frame, ms |
|-------+------------+------------------|
| construction | 1.602 | 0.336 |
| first demand | 0.348 | 0.116 |
| warm combination calls | 1756.113 | 1732.496 |
| native single-method baseline | 0.062 | 0.059 |

Profiling 1000 frame constructions plus unchanged native type validation found
514.672 ms with the dynamic slot constructor versus 7.415 ms using one static
prototype with native constant defaults. The internal =make-frame= implementation
was changed accordingly; no contract or qualifier/next check was disabled.
This isolated microbenchmark improvement did NOT translate into a material
end-to-end warm improvement. Additional probes varied considerably with depth
and callback shape; no claim about the remaining bottleneck is made here.
The static constructor follows the project value-family rule, but high-frequency
production performance remains unqualified. Both full runs preserved checksum
=507500=, =plan-reused? #t= and =within-budget? #t=.
The 10000 ms scenario threshold is only a bounded smoke budget, not evidence
that generic-combination overhead is comparable to a single native method.

* Scheme-native test ownership and preflight
All method-combination qualification entrypoints now live under
=t/qualification/poo-method-combination/=. No duplicate =tools/= entry or shell
implementation remains. =qualify.ss= runs =preflight.ss= before starting native
compilation, then =run.ss= for same-process artifact admission, compiled suites
and measurement. =summary.ss= is a read-only native statistical projection.

The native preflight has 24 passing assertions: 14 I/O/fingerprint checks and
10 statistical-summary checks. Representative evidence includes exact main and
=~0= module manifest lines written only to the file, identical explicitly
captured log/console bytes, SHA-256 of =abc= retained after the file is changed
to =def= before any fingerprint slot is demanded, and rejection of that drift.
The summary fixture computes median =3= and range =2..4= and rejects empty,
duplicated or inconsistent trial records. These checks are separate from the
203 semantic/regression assertions, not additional AOT semantic coverage.

One expanded migration run compiled all 22 requested modules but failed before
test execution: an incorrect positional use of =displayln= left =modules.txt=
empty. The artifact guard rejected it and no success receipt was written. The
corrected implementation uses =(display value port)= and =(newline port)=;
the I/O preflight now tests this boundary before every full run. Other earlier
migration failures and the intentionally interrupted old-directory compilation
are not qualified artifacts or successful AOT runs.

* Current successful test-owned native receipt
The complete =qualify.ss= command exited 0. Evidence directory:
#+begin_src text
.data/qualification/poo-method-combination/run.d99481b5-289c-4120-ba7e-fff8cb255660/
#+end_src

| Native receipt field | Observed value |
|----------------------+----------------|
| =schema= / =producer= | =v2= / =poo-flow/modules/poo-method-combination= |
| =accepted?= | =#t=, local dependent-slice qualification only |
| =preflight-assertions= | 24 |
| =assertions= | 265 across 7 compiled suites |
| =source-count= | 35: 28 compiled inputs plus 7 qualification/helper/test sources |
| =artifact-count= | 58 actual fresh =.o1= paths, including implementation/phase units |
| =same-process-tests?= | =#t= |
| repeated measurement rows | 80: 16 configurations times 5 trials |

All 28 requested inputs produced nonempty =.ssi= and =.o1= artifacts. The exact
path check, seven compiled suites and matrix executed in one interpreter.
Owned sources and compiled artifact fingerprints remained unchanged through
the run. Twenty artifact fingerprints for seven named upstream dependencies
and their implementation units were checked before/after execution. This is
not a claim to fingerprint every transitive dependency or prove reproducible
binaries across hosts.

The final =receipt.sexp= binds =sources.sexp=, =artifacts.sexp=, =toolchain.sexp=,
=modules.txt=, =dependencies.sexp=, =preflight.log= and =qualification.sexp= with
native SHA-256 records tagged =algorithm= and =purpose=. The source and compiled
203-assertion runs overlap; they are not 406 distinct checks. The 24 preflight
assertions are test-infrastructure coverage, not extra method semantics.

Selected exact manifest hashes copied from the terminal receipt:
| Manifest | SHA-256 |
|----------+---------|
| =sources.sexp= | =6d694872df931235c4c0295a302e7464dd6c3914e67686b4419d828e4ed03a5d= |
| =artifacts.sexp= | =0f8ae215a4b6517c86de2e7a0804aad198d9c70c75d7d5118196db21351edf33= |
| =qualification.sexp= | =1f154b7b55d8f744493e9b3d0f50a349cec2916de9b10b90ec2fba1ca5bc4287= |

* Repeated measurements: what is and is not established
The native =summary.ss= command exited 0 and produced 16 summaries. Each row
is also retained in the run directory's =performance-summary.sexp= as a derived
projection; the original acceptance input remains the fingerprinted raw
=qualification.sexp=. Each table row
below describes five 100-call batches. Times are milliseconds; allocations are
bytes per batch, not RSS or peak memory. The comparator is the equivalent
functional chain described in [[file:V1-CONTRACT.org][the v1 contract]], not the historical
single-method arithmetic shortcut. The comparator does not implement POO's
admission/frame contracts. Its very small timings approach clock resolution,
so large ratios must not be presented as portable speedup/slowdown factors.

| Depth / owner / qualifiers | Combination median | Min..max | Functional median | Median allocated bytes |
|----------------------------+--------------------+----------+-------------------+------------------------|
| 0 / instance / primary | 0.215 | 0.214..26.962 | 0.000954 | 305808 |
| 1 / instance / primary | 3.287 | 0.611..145.896 | 0.001192 | 974608 |
| 4 / instance / primary | 4.341 | 2.321..306.204 | 0.002146 | 2981008 |
| 8 / instance / primary | 25.758 | 9.345..383.455 | 0.001907 | 5656208 |
| 8 / instance / all | 125.883 | 73.467..991.200 | 0.006914 | 12785808 |
| 8 / type / primary | 43.284 | 5.659..535.320 | 0.005960 | 5662608 |
| 8 / type / all | 135.287 | 14.072..510.899 | 0.008106 | 12792208 |

Every trial retained exact checksum and plan identity. There is substantial
wall-time variation even within one configuration. In the depth-8 type/primary
case all five samples reported zero GC count/time despite a 5.659..535.320 ms
range and identical allocations. Some other cases did collect: depth-8
instance/all had a maximum measured GC wall time of 665.415 ms. Therefore GC
alone is not established as the explanation for all timing variation. CPU-time
and execution profiling are still needed before attributing a hotspot or making
another optimization claim. The increasing allocation cost is observed evidence,
not yet a proof of which internal constructor or validation step owns it.

The separate historical-shape 1000-call, depth-8 sample measured construction
0.330 ms, first demand 0.116 ms and warm calls 1605.435 ms, with checksum
=507500= and identical cached plan. No execution-core optimization was made in
this continuation, and no performance win is claimed from cross-run timing.
Production call-rate/latency/allocation budgets are still unspecified. Local
semantic/AOT qualification does not grant production performance admission.

* Current owned source SHA-256
| Source | SHA-256 |
|--------+---------|
| types.ss | fa539cbfa3231293ad3d9c6263e9cd073aa9692a047fe7f7cecd4bc2701167cd |
| objects.ss | 113e01ec1298c2fdeaeacbbb7ba2b787424423884d1d9356bf241d18df8baf40 |
| func.ss | b25fb6ce1230a744ce865f6e0403dcda5ca208dc173f6887378b4cb25933aa2b |
| interface.ss | 4a7aebe5f73cf84680fc42295984399fd08857ed324f15359b93130300545f07 |

* Current compiled main artifact SHA-256
| Artifact | SHA-256 |
|----------+---------|
| types.o1 | 40e88e3dccd84fdb26938bddc8ddcab8e4132390a50d4cae34c9b74f323eb68a |
| objects.o1 | ab8c7392a37cad8be5fff195d3312e957323f880eafbdd273a6f03f05c95563f |
| func.o1 | f2c190e22059822c189a06b946fa60d56689f0d716bcbd92971fdfc5fd34c560 |
| interface.o1 | e65e8cf0130cc7dee2d60806eba44add32803df71eef79a7d3e64e67e81bc997 |

* Boundaries still outside this receipt
All five local test targets are registered in =scheme/BUILD.bazel=; buildifier
and shell syntax checks passed. No =bazel= or =bazelisk= executable was available
in the checked environment, so Bazel execution was not performed. The shared
workspace contains other changes; this receipt does not qualify those changes
or replace a full-package build. Installed upstream dependency provenance and
an upstream API review remain separate from this owned-library AOT check.
