#+title: Gerbil-Bazel PR 29 Downstream Performance Receipt
#+author: POO Flow maintainers
#+date: 2026-07-25
#+startup: overview

* Status

Historical receipt; its measurements remain evidence, but its former audit
entrypoints are not current interfaces.

Performance policy is independent of Bazel.  Current cold/warm measurements
come from the native =gxpkg= and Building Framework lifecycle; the pure POO
Flow comparison consumes those immutable observations.  Bazel remains a
hermetic integration gate.  POO Flow does not wrap, recursively launch, clean,
or shut down Bazel in a performance-audit runner.

#+begin_src mermaid
flowchart LR
  Native["Native gxpkg cold and warm builds"] --> Receipt["Building Framework observation"]
  Receipt --> Policy["Bazel-independent POO performance policy"]
  Bazel["Bazel integration tests"] -. "tests policy and build graph" .-> Policy
  Policy --> Decision{"Comparable and within budget?"}
  Decision -->|yes| Accept["Accept evidence"]
  Decision -->|no| Reject["Regression or incomparable"]
#+end_src

All later references in this file to
=//scheme:compile_performance_audit_test= or
=poo-flow.project-compile-guard.v1= describe historical executions only.  They
must not be restored as compatibility targets or current receipt ownership.

At the time of this measurement the downstream build was blocked by regression.

This receipt extends the convergence boundary in
=155-gerbil-bazel-downstream-convergence.org=.  It records the first POO Flow
external-module build pinned to gerbil-bazel commit
=aa370bb60076745835cf81e8d1e6a48a95006913= from draft PR 29.

* Validation identity

| Field | Value |
|-------+-------|
| POO Flow entrypoint | =just test-external-bazel-module= |
| gerbil-bazel revision | =aa370bb60076745835cf81e8d1e6a48a95006913= |
| Gerbil version | =0.18.2= |
| Gerbil revision | =07c84815= |
| Platform | =darwin-arm64= |
| Logical and requested cores | 12 |
| External test root | =/private/var/folders/pj/n33bqzy94yn9wxggj3rny0d00000gn/T/tmp.M6I0fEws5V= |
| Bazel output identity | =9f2e128949159c5c207071eaa228cb97= |
| Final command outcome | interrupted after the POO package crossed its performance gate |

No =local_path_override= participated in this validation.  The test consumed a
remote, commit-addressed =git_override=.

* Package receipts

** gerbil-utils

| Field | Value |
|-------+-------|
| Project duration | 57 seconds |
| Guard elapsed | 54,995 ms |
| Accepted baseline | 42,364 ms |
| Regression | 29.8 percent |
| Effective cores | 12 |
| Peak RSS | 1,964,687,360 bytes |
| Runnable core limit applied | false |
| Outcome | completed, but performance gate failed |

The accepted maximum regression is five percent.  This action therefore fails
the convergence gate even though its functional build succeeded.

** gerbil-poo

| Field | Value |
|-------+-------|
| Project duration | 101 seconds |
| Guard elapsed | 98,108 ms |
| Performance gate | 90,000 ms |
| Gate excess | 9.0 percent |
| Effective cores | 12 |
| Peak RSS | 4,551,016,448 bytes |
| Runnable core limit applied | false |
| Outcome | completed, but performance gate failed |

The complete external consumer was intentionally interrupted before the next
package could hide or amortize this regression.

* Root-cause boundary

The gerbil-bazel RFC 0008 audit rejected two speculative library changes:

1. Apple Clang was approximately 95.7 percent slower than the producer
   compatible GCC 16 compiler for the same generated object input.
2. Twelve concurrent GCC object jobs completed in 11,466 ms with a peak RSS of
   4,614,832,128 bytes, so a smaller fixed macOS core cap is not justified.

A native =gxc= probe over the same sixteen Gerbil modules crossed the
90-second guard without Bazel staging or =build.ss= orchestration.  The
remaining owner is the gerbil-poo frontend workload: module dependency depth,
macro expansion, and cross-module optimization.

#+begin_src mermaid
flowchart TD
  Pin["POO Flow pins gerbil-bazel aa370bb"] --> Utils["gerbil-utils action"]
  Utils --> UtilsGate{"at most 44,482 ms"}
  UtilsGate -->|"54,995 ms: fail"| Stop1["Regression recorded"]
  Utils --> Poo["gerbil-poo action"]
  Poo --> PooGate{"at most 90,000 ms"}
  PooGate -->|"98,108 ms: fail"| Stop2["External build interrupted"]

  Stop1 --> Library["gerbil-bazel scheduling hypotheses rejected"]
  Stop2 --> Owner["gerbil-poo module and macro graph is next owner"]
#+end_src

* Promotion decision

gerbil-bazel PR 29 must remain draft.  The POO Flow pin must not be described
as a performance improvement or promoted as a stable downstream convergence
revision while the optimized gerbil-poo package gate and the full POO Flow
compile gate fail.

The adaptive resource guard itself remains valid:

- it used Gerbil =__available-cores= as the CPU-capacity source;
- it admitted twelve cores within the observed memory budget;
- it did not apply the runnable-pressure diagnostic as a hard cap;
- it emitted complete typed receipts for both packages.

The next implementation phase belongs to the POO Flow and GSLPH Scheme build
owners.  gerbil-bazel must remain the reusable toolchain and project-action
library.  It must not compensate by parsing =build.ss=, inventing a second
package manager, or deriving a private POO-specific module graph.

* Rejected optimization experiments

** Production/test spec split

A clean hermetic gerbil-poo archive was rebuilt after removing
=t/table-testing= from the production =build.ss= spec.  The resulting native
spec contained fifteen library modules and contained neither
=t/table-testing= nor the ignored generated =version.ss=.

The guarded build completed, but it did not improve the gate:

| Candidate | Guard elapsed | Peak RSS | Outcome |
|-----------+---------------+----------+---------|
| optimized baseline | 98,108 ms | 4,551,016,448 bytes | over 90-second gate |
| production/test split | 104,755 ms | 4,658,020,352 bytes | slower; rejected |

The gerbil-poo source edit was reverted.  No binary or generated vector was
added to Git.

** Unoptimized development dependency

The existing declarative =args= interface was used to form the native command
=gxi build.ss compile --no-optimize=.  The real Bazel package action completed
in 79,769 ms with a peak RSS of 3,793,813,504 bytes.  That is 18,339 ms
(18.7 percent) faster than the optimized 98,108 ms package baseline and passes
the package time gate.

This result is not promotable.  The formal =//scheme:unit_tests= gate ended in
a segmentation fault after 39.1 seconds.  Disabling optimization therefore
cannot be the default development or release contract until the ABI failure is
root-caused independently.  The POO Flow =args= edit was reverted.

* Full POO Flow critical-path receipt

The formal test run materialized a complete project compile receipt before the
test process failed.  The compile action completed in 301,093 ms with twelve
workers and a peak RSS of 4,505,632,768 bytes.

| Stage | Spec count | Elapsed |
|-------+------------+---------|
| nono-c-ffi | 2 | 1,100,477 jiffies |
| runtime | 381 | 294,239,987 jiffies |
| user-interface | 8 | 5,391,686 jiffies |

The runtime stage accounts for approximately 97.7 percent of the guarded
compile duration.  Optimizing only gerbil-utils or gerbil-poo cannot solve this
end-to-end critical path.

#+begin_src mermaid
flowchart LR
  Spec["POO Flow build spec (391 specs)"]
  FFI["nono-c-ffi (2 specs)"]
  Runtime["runtime stage (381 specs)"]
  UI["user-interface (8 specs)"]
  Layers["GSLPH topology layers"]
  Process["one gxi process"]
  Optimizer["Gerbil optimize! under global driver mutex"]
  Backend["pending Gambit/C jobs"]
  Receipt["301,093 ms compile receipt"]

  Spec --> FFI
  Spec --> Runtime
  Spec --> UI
  Runtime --> Layers
  Layers --> Process
  Process --> Optimizer
  Process --> Backend
  Optimizer --> Receipt
  Backend --> Receipt
#+end_src

* Upstream-compatible ownership boundary

Gerbil upstream already provides parallel workers in =std/make=.  Its compiler
driver also deliberately protects =optimize!= with a process-global driver
mutex, and =std/make= executes pending Gambit/C jobs after the module build
barrier.  The observed frontend serialization is therefore an upstream
execution property, not evidence that Bazel failed to pass the host CPU count.

GSLPH currently computes dependency topology layers, but
=build-requests-run!= executes package stages sequentially and each POO runtime
layer remains inside the same gxi process.  The next design must preserve the
upstream compiler and move parallelism to explicit project-owned component
boundaries:

#+begin_src mermaid
flowchart LR
  Manifest["POO/GSLPH declarative component manifest"]
  DAG["Component dependency DAG"]
  A["Component action A: native std/make"]
  B["Component action B: native std/make"]
  C["Component action C: native std/make"]
  Guard["Scheme process-tree resource guard"]
  Artifacts["Atomic component artifacts and receipts"]
  Consumer["POO runtime/test consumers"]

  Manifest --> DAG
  DAG --> A
  DAG --> B
  DAG --> C
  A --> Guard
  B --> Guard
  C --> Guard
  Guard --> Artifacts
  Artifacts --> Consumer
#+end_src

The project or GSLPH must declare the component graph.  gerbil-bazel may
execute those declared Gerbil projects and cache their outputs, but it must not
parse Scheme source or infer POO domain ownership.  Release builds must keep
Gerbil optimization enabled, all worker limits must remain host-derived, and
promotion requires both green unit tests and end-to-end performance receipts.

* Current dependency ownership correction (2026-07-27)

The former independent =dependency_source_contract_sources= package and
=src/contract/dependency-source-identity.ss= module were removed. They
duplicated Gerbil package declarations and the Gerbil-Bazel hermetic build
lock; they are not a current POO Flow source-ownership boundary.

=gerbil.pkg= owns package declarations. Gerbil-Bazel owns hermetic
materialization, source-resolution receipts, and their generic validation.
POO Flow uses =gerbil_project_compile= directly without maintaining a second
dependency catalog or receipt validator.

* Historical source-ownership parity correction (superseded 2026-07-27)

The local dynamic source scan initially reported 382 runtime specs while the
hermetic Bazel receipt reported 381.  This was not a missing Bazel source.  The
module =src/contract/dependency-source-identity.ss= is intentionally owned by
the independent =dependency_source_contract_sources= package and excluded from
=scheme_runtime_sources=.  The local Scheme predicate did not express that
ownership boundary and therefore pulled the module back into the monolithic
runtime spec.

=+poo-flow-project-runtime-module-owners+= now declares both separately owned
modules as a pure module-to-owner manifest.  The runtime predicate projects
from that manifest instead of maintaining an unrelated negative list:

- =contract/dependency-source-identity.ss=, compiled by the dependency-source
  contract package;
- =modules/nono-sandbox/_nono.ss=, compiled by the C FFI stage.

The focused =cli-support-project-building-test.ss= gate passed with all checks.
The native topology projection now agrees with the hermetic receipt at exactly
381 runtime specs.

* Runtime topology feasibility projection

The corrected graph has 39 dependency layers.  Its maximum layer width is 57;
16 layers contain at least six modules and 12 layers contain at least twelve
modules.  Six layers are singletons.

| Property | Value |
|----------+-------|
| runtime specs | 381 |
| dependency layers | 39 |
| maximum layer width | 57 |
| layers at least 6 wide | 16 |
| layers at least 12 wide | 12 |
| singleton layers | 6 |

This proves that process-level optimized compilation has useful parallel width.
The implementation still needs dependency-preserving execution windows and
must not flatten all 39 layers into one unordered pool.

* Persistent optimized worker model

Starting one gxi process per module is rejected.  A representative import of
the GSLPH building surface costs approximately 3,715 ms before useful module
work begins.  Repeating that startup for hundreds of modules would dominate
the compile.

The required execution model is a fixed, host-derived pool of long-lived gxi
workers:

#+begin_src mermaid
flowchart LR
  Plan["POO/GSLPH topology plan (39 layers)"]
  Parent["Scheme parent scheduler and RSS guard"]
  Queue["Current-layer module queue"]
  W1["gxi worker 1 with independent optimizer"]
  W2["gxi worker 2 with independent optimizer"]
  WN["gxi worker N with independent optimizer"]
  Barrier["Layer artifact and ssxi barrier"]
  Receipt["Per-layer and per-worker JSON receipts"]

  Plan --> Parent
  Parent --> Queue
  Queue --> W1
  Queue --> W2
  Queue --> WN
  W1 --> Barrier
  W2 --> Barrier
  WN --> Barrier
  Barrier -->|"next layer"| Queue
  Parent --> Receipt
  W1 --> Receipt
  W2 --> Receipt
  WN --> Receipt
#+end_src

Each worker owns a separate Gerbil compiler process and therefore a separate
driver mutex.  Modules in one dependency layer may run concurrently, while the
next layer cannot start until the current layer has materialized its library
and =ssxi= artifacts.  The parent Scheme guard must monitor the complete worker
process tree and may reduce active workers from current host observations; the
worker count must never be hard-coded.

For the observed 381-module graph and twelve available workers, the 39 layers
reduce to 57 ideal equal-cost worker waves.  Using the observed runtime-stage
average of 772.3 ms per module gives a theoretical, startup-free lower bound of
approximately 44,020 ms, or 6.68 times faster than the 294,240 ms runtime
stage.  This is a feasibility bound, not a performance claim: real promotion
requires measured receipts that include skew, worker startup, artifact
barriers, RSS, and failure cancellation.

* Topology-batching correction and repeatable cold-audit receipt

The first persistent-worker candidate did not preserve the existing topology
policy boundary.  The POO Flow guard selected =topology=, not
=adaptive-window=, but =package-source-stage-request-specs/default= returned
each topology group as a separate upstream request.  The 381-module runtime
stage was therefore materialized as 40 runtime =std/make= stages, with three
additional FFI and user-interface stages.  The failure was not caused by the
persistent worker pool: every affected receipt reported
=adaptive-execution=false=.

GSLPH revision =a0ba60a15dd05d95ea89c0506f530f426d3dca1e= restores
=package-source-stage-topology-request-specs= as the =topology= batching
boundary.  Its hermetic GitHub archive digest is
=sha256:de202ddb3b204f4faa461f44df0cfeaa3bea5d77b047e46babc9a1fa51b84838=.
The focused GSLPH suite passed 11 test cases and 30 checks, including a
two-layer public-API fixture that requires one upstream package stage.

The same-host downstream diagnostic produced the following complete receipts:

| Revision | Stages | Internal elapsed | Peak RSS | Outcome |
|----------+--------+------------------+----------+---------|
| =9ca9b7a168cf1b3e19622b539a19acbf38263567= | 43 | 358,753 ms | 3,663,937,536 bytes | rejected |
| =a0ba60a15dd05d95ea89c0506f530f426d3dca1e= | 3 | 265,809 ms | 3,299,360,768 bytes | diagnostic accepted |

The corrected diagnostic is 25.9 percent faster than the rejected candidate,
while reducing peak RSS by 9.9 percent.  Relative to the earlier 3-stage
single-sample reference of 333,763 ms and 4,262,494,208 bytes, it is 20.4
percent faster and uses 22.6 percent less peak RSS.  These figures are
single-sample diagnostics; promotion still requires the formal odd-sample
median gate.

The cold-audit runner previously created a distinct Bazel output base for
every sample.  Gerbil package action keys contain stabilized paths derived
from that output base, so the supposedly shared dependency disk cache could
not match the next sample.  The corrected runner owns one stable output base
per comparison side, invokes Bazel-native =clean= before each sample, restores
the three dependency packages from one shared dependency cache, and gives
only the root compile action a sample-local disk cache.  It also persists the
parsed receipt immediately as =SIDE-N/compile.receipt.json= before the next
clean, rather than retaining a symlink into mutable Bazel output state.

#+begin_src mermaid
flowchart LR
  Side["One stable output base per side"]
  Clean["Bazel-native clean before each sample"]
  Seed["dependency_packages with shared disk cache"]
  Hits["Three package cache hits in 3.714-6.101 s"]
  Cold["compile_receipt with sample-local root cache"]
  Immutable["Immutable SIDE-N/compile.receipt.json"]
  Compare["Odd-sample median budget gate"]
  Crash["Candidate sample 3: gxi SIGBUS, exit 138"]
  Blocked["Promotion blocked; no comparison acceptance"]
  Guard["Required next gate: failed-build guard receipt"]

  Side --> Clean
  Clean --> Seed
  Seed --> Hits
  Hits --> Cold
  Cold --> Immutable
  Immutable --> Compare
  Cold --> Crash
  Crash --> Blocked
  Blocked --> Guard
#+end_src

The real audit confirmed that repeated dependency seeding fell from 336.871
seconds for the first baseline side and 342.421 seconds for the first
candidate side to 4.019, 6.101, 3.714, and 5.603 seconds for subsequent
samples.  Each subsequent seed reported three disk-cache hits, a 98.2-98.9
percent reduction in repeated seed wall time.

The formal three-sample promotion gate remains blocked.  All three baseline
cold actions completed in 288.523, 313.690, and 320.398 seconds of Bazel wall
time.  Candidate samples 1 and 2 completed in 303.224 and 314.752 seconds.
Candidate sample 3 terminated after 108.226 seconds with macOS
=Bus error: 10= from =gxi=, producing exit 138.  No median comparison receipt
was emitted, so the dependency pin is not promoted by this record.

This failure also exposes an observability gap: the Scheme compile guard emits
receipts for pre-admission rejection and successful completion, but not for an
exception raised by the guarded build.  Before the formal audit is repeated,
the guard must emit a terminal failed-build receipt containing the stopped
process-tree guard observation, elapsed time, peak RSS, admission snapshot,
sample identity, and failure classification.  A retry without that evidence
would be an unqualified repetition rather than a new performance result.

* Scheme supervisor boundary for fatal child termination

The observability gap is now closed at the process boundary rather than with
an exception handler inside the compiling =gxi=.  An in-process handler cannot
emit a receipt after =SIGBUS= terminates its process.  The public =compile=
entry therefore remains in a parent Scheme process, starts the existing
Scheme process-tree memory guard, and launches an internal =compile-worker=
entry as a child =gxi=.  The worker name is not advertised as a public build
entry.

The parent classifies a zero child exit as =completed= and every non-zero exit
as =child-failed=.  Because the parent survives a fatal child signal, it can
emit a terminal =poo-flow.project-compile-guard.v1= receipt before raising the
build failure.  The failed receipt carries =failure-kind=,
=failure-exit-code=, peak RSS, elapsed milliseconds, timeout state, and the
original host-admission snapshot.

All project-compile receipts now share one stable POO object shape.
=completed= and =blocked-host-pressure= receipts contain
=failure-kind=#f= and =failure-exit-code=#f=; =failed-build= replaces those
values with the process-guard classification.  JSON projection uses ordinary
POO slot access for this declared schema.  It no longer misuses =.has?=,
which does not report dynamic =.o= slots and previously erased the failure
classification.

#+begin_src mermaid
flowchart TD
  Bazel["Bazel Gerbil project action"]
  Parent["Parent Scheme compile supervisor"]
  Admit{"Host admission ready?"}
  Blocked["Emit blocked-host-pressure receipt"]
  Guard["Start Scheme process-tree memory guard"]
  Worker["Child gxi: internal compile-worker"]
  Upstream["Building Framework and upstream std/make"]
  Exit{"Child exit code"}
  Success["Child completed receipt"]
  Failure["Parent emits failed-build receipt"]
  Raise["Raise build failure to Bazel"]

  Bazel --> Parent
  Parent --> Admit
  Admit -- "No" --> Blocked
  Admit -- "Yes" --> Guard
  Guard --> Worker
  Worker --> Upstream
  Upstream --> Exit
  Exit -- "0" --> Success
  Exit -- "non-zero or fatal signal" --> Failure
  Failure --> Raise
#+end_src

The real Bazel gate exercised this parent-to-child lifecycle while rebuilding
the changed Scheme project.  =bazelisk test //scheme:unit_tests= completed
successfully with one of one test targets passing.  Bazel reported 558.546
seconds total elapsed time, a roughly 402-second root compile action, a
119.6-second test action, 32 action-cache hits, and no dependency-package
rebuild.  This is correctness and observability evidence, not a replacement
for the blocked three-sample cold-performance median gate.  The formal
baseline-versus-candidate audit must still be repeated before the GSLPH
dependency pin can be promoted.

* Audit-server lifecycle and canonical output-root correction

The first supervised formal rerun was deliberately stopped before any cold
sample receipt was accepted.  Its audit driver target finished analysis in
320.721 seconds with four action-cache hits, then the baseline output-base
server remained unavailable for more than 80 seconds.  The audit tree
contained a server PID and output-base state but no immutable sample receipt.
That interval is infrastructure startup noise and must not be counted as a
Gerbil cold compile.

A host-process audit found multiple Bazel JVMs left by earlier probe, baseline,
and candidate output bases.  Their configured idle lifetime was 10,800
seconds.  Even read-only =gxi= and filesystem probes then required 60-155
seconds, demonstrating that a benchmark retry on that host state would be
invalid.  The obsolete servers were shut down through Bazel's native
=shutdown= command; only an unready server from an interrupted focused test
required a targeted =TERM= after the client could not connect.

The Scheme audit runner now owns two additional invariants:

1. It creates the requested output directory and resolves its physical path
   through Gerbil/Gambit's parameterized =current-directory=, which delegates
   to the operating system's =chdir/getcwd= behavior.  On Darwin this
   canonicalizes =/tmp= to =/private/tmp= without a shell parser.
2. The audit explicitly owns one stable Bazel server per side for the whole
   alternating sample sequence.  A pre-armed lifecycle receipt declares a
   =dynamic-wind= cleanup boundary, and both success and failure unwind paths
   issue Bazel's native =shutdown=.  No machine-independent idle timeout is
   hard-coded, so the opposite side's multi-minute sample cannot evict a
   server that the current audit still owns.

The canonical physical output directory is recorded in
=gerbil-bazel.compile-performance-audit.v1= as =outputDirectory=.  The focused
fixture passes a symlink alias and requires the receipt to contain =pwd -P='s
physical directory.  It also requires both side output bases to receive a
native shutdown on the successful audit path and on a synthetic fatal sample
path.

#+begin_src mermaid
flowchart TD
  Input["Requested audit output path"]
  Canonical["Scheme canonical-directory via chdir/getcwd"]
  Physical["One physical output root"]
  Baseline["Baseline stable output base"]
  Candidate["Candidate stable output base"]
  Armed["Pre-armed server-lifecycle receipt"]
  Samples["Three cold samples per side"]
  Receipt["Immutable sample and audit receipts"]
  Exit["Completed or interrupted audit"]
  Cleanup["Scheme dynamic-wind cleanup"]
  Reclaim["Native Bazel shutdown for both side servers"]

  Input --> Canonical
  Canonical --> Physical
  Physical --> Baseline
  Physical --> Candidate
  Physical --> Armed
  Baseline --> Samples
  Candidate --> Samples
  Armed --> Samples
  Samples --> Receipt
  Receipt --> Exit
  Exit --> Cleanup
  Cleanup --> Reclaim
#+end_src

After host recovery, the focused Bazel gate
=//scheme:compile_performance_audit_test= passed one of one targets.  The
final explicit-lifecycle run completed in 7.4 seconds and covered successful
and failed unwind paths.  This qualifies the runner-lifecycle correction only.
It does not qualify the GSLPH candidate: the fresh three-sample formal
baseline-versus-candidate audit remains the next required gate.

* Supervised formal rerun: terminal failure receipt and remaining blocker

The next formal rerun used physical output root
=/private/tmp/poo-flow-gslph-a0ba60a-formal-20260726= and host session
=host-session/2026-07-26-darwin-arm64=.  Both sides were pinned to POO Flow
commit =4fff1e74=, their three shared gerbil-bazel configuration files were
byte-identical, and the only intended variable was the GSLPH revision.

The completed partial observations were:

| Side | Sample | Dependency seed | Root cold action | Outcome |
|------+--------+-----------------+------------------+---------|
| baseline | 1 | 313.578 s | 449.925 s | completed |
| candidate | 1 | 350.786 s | 301.404 s | completed |
| baseline | 2 | 4.112 s | 401.600 s | completed |
| candidate | 2 | 7.008 s | 85.741 s internal; 139.262 s Bazel | failed-build |

The second seed on each side reported three disk-cache hits.  Cross-workspace
seed reuse is still incomplete: the first candidate seed rebuilt the package
actions instead of reusing the first baseline seed.  This is a cache-key
optimization target, not part of the GSLPH root-compile comparison.

Candidate sample 2 terminated its child =gxi= with signal/exit value 10.
Unlike the earlier SIGBUS run, the parent Scheme supervisor survived and
emitted a complete terminal project receipt:

| Field | Observed value |
|-------+----------------|
| outcome | =failed-build= |
| failure-kind | =child-failed= |
| failure-exit-code | 10 |
| elapsed-ms | 85,741 ms |
| peak-rss-bytes | 3,079,274,496 bytes |
| runnable-process-count | 44 |
| admission-advisory | =runnable-saturation= |
| worker-count | 12 |

This proves the supervisor boundary, but the audit runner still aborted before
copying the terminal line from Bazel output into the sample directory.  The
runner now captures merged Bazel stdout/stderr and persists three immutable
failure artifacts before raising:

- =SIDE-N/compile.receipt.json= contains the native Scheme supervisor receipt;
- =SIDE-N/failure.context.json= contains side, sample index, revision, host
  session, runner, raw process status, receipt path, and log path;
- =SIDE-N/build.log= contains the complete Bazel output used to extract the
  receipt.

#+begin_src mermaid
flowchart TD
  Child["Child gxi terminates with signal 10"]
  Parent["Parent Scheme supervisor survives"]
  ProjectReceipt["Emit POO_FLOW_PROJECT_BUILD_RECEIPT"]
  Bazel["Bazel returns non-zero"]
  Capture["Audit runner captures stdout and stderr"]
  Native["Immutable compile.receipt.json"]
  Context["Immutable failure.context.json"]
  Log["Immutable build.log"]
  Abort["Raise sample failure and block comparison"]
  Promote["GSLPH pin promotion"]

  Child --> Parent
  Parent --> ProjectReceipt
  ProjectReceipt --> Bazel
  Bazel --> Capture
  Capture --> Native
  Capture --> Context
  Capture --> Log
  Native --> Abort
  Context --> Abort
  Log --> Abort
  Abort -. "blocked" .-> Promote
#+end_src

The focused synthetic-failure gate
=//scheme:compile_performance_audit_test= passed one of one targets in 8.8
seconds.  It requires the three failure artifacts and verifies the preserved
native fields =failure-exit-code= with value 10 and =outcome= with value
=failed-build=.

The formal promotion gate remains blocked for two independent reasons:

1. candidate sample 2 failed, so no three-sample median exists;
2. both completed baseline cold actions, 449.925 and 401.600 seconds, exceed
   the previous baseline median of 313.690 seconds by approximately 43.4 and
   28.0 percent respectively.

No GSLPH dependency promotion is authorized by this receipt.  The next run
must start from a host admission state without runnable saturation, preserve
the new failure artifacts if any sample terminates, and produce all six cold
observations before the relative median gate is evaluated.

The native Scheme receipts distinguish compile time from Bazel wall time:

| Side/sample | Scheme elapsed | Bazel wall | Bazel overhead | Peak RSS | Runnable snapshot |
|-------------+----------------+------------+----------------+----------+-------------------|
| baseline-1 | 364,168 ms | 449,925 ms | 85,757 ms | 5,769,314,304 bytes | 32, saturated |
| candidate-1 | 245,911 ms | 301,404 ms | 55,493 ms | 4,988,354,560 bytes | 59, saturated |
| baseline-2 | 352,891 ms | 401,600 ms | 48,709 ms | 4,816,371,712 bytes | 16, admitted |

The incomplete observations therefore support two separate statements.  The
candidate's only completed Scheme compile was 32.5 percent faster than
baseline-1 and 30.3 percent faster than baseline-2.  The formal gate still
cannot accept a one-sample candidate, while the 48.709-85.757 second Bazel
overhead must not be attributed to Scheme compilation.

The fixed 30-second server timeout used during this interrupted run caused a
side server to expire while the opposite side compiled.  That design has been
removed.  The next formal run will keep both stable side servers alive for the
entire alternating sequence and reclaim them explicitly through
=dynamic-wind=, eliminating avoidable per-sample server restart and analysis
overhead without retaining JVMs after the audit.

* Per-sample host admission

The incomplete formal observations also prove that a single audit-start
preflight is insufficient.  Runnable pressure changed from 16 to 59 while the
alternating sequence was active.  A comparison runner must therefore prove
host admissibility immediately before every baseline and candidate cold
sample, before dependency seeding or compilation consumes several minutes.

The runner now consumes the resource guard owned by gerbil-bazel through the
declared Bazel runfile =@gerbil_bazel//gerbil:resource_guard.ss=.  POO Flow
does not duplicate operating-system probes, CPU discovery, memory discovery,
or process-tree supervision.  The Bazel-resolved guard imports Gerbil
=__available-cores= and emits its normal immutable resource-guard receipt.
The audit projects that receipt into
=poo-flow.ci.scheme-compile-performance-host-admission.v1=.

The comparison-only admission rule is stricter than the normal build rule:

- the upstream resource guard must report =admissionOutcome= equal to
  =ready=;
- =runnableProcessCountAvailable= must be true;
- =runnableProcessCount= must be at or below the observed
  =logicalCpuCount=.

The second condition is machine-adaptive.  It contains no fixed core count,
fixed wait interval, or operating-system-specific shell probe.  Normal project
compilation still treats runnable saturation as advisory and may adapt its
worker plan.  Only the formal comparison runner rejects a sample whose timing
would not be attributable to the revision under test.

Each sample directory now owns:

- =resource-guard.receipt.json=, the upstream capability receipt;
- =host-admission.json=, the comparison-specific decision and provenance;
- =failure.context.json= with =phase= equal to =host-admission= when the
  sample is rejected.

A rejected sample never starts dependency seeding, native ABI discovery, or
the cold compile action.  The enclosing =dynamic-wind= still shuts down both
owned Bazel servers.

#+begin_src mermaid
flowchart TD
  Sample["Next baseline or candidate sample"]
  Guard["Run declared gerbil-bazel Scheme guard with fresh ps state"]
  NativeReceipt["Persist resource-guard.receipt.json"]
  Ready{"Guard admission ready?"}
  Available{"Runnable observation available?"}
  Capacity{"Runnable count <= observed logical CPU count?"}
  Admission["Persist ready host-admission.json"]
  Cold["Seed dependencies and run cold compile"]
  Blocked["Persist blocked host-admission.json"]
  Context["Persist failure.context.json with host-admission phase"]
  Unwind["Unwind audit"]
  Shutdown["Native shutdown of both owned Bazel servers"]

  Sample --> Guard
  Guard --> NativeReceipt
  NativeReceipt --> Ready
  Ready -- "yes" --> Available
  Ready -- "no" --> Blocked
  Available -- "yes" --> Capacity
  Available -- "no" --> Blocked
  Capacity -- "yes" --> Admission
  Capacity -- "no" --> Blocked
  Admission --> Cold
  Blocked --> Context
  Context --> Unwind
  Unwind --> Shutdown
#+end_src

The focused Bazel gate =//scheme:compile_performance_audit_test= passed one of
one targets in 29.1 seconds, within its unchanged 60-second test timeout.  Its
pressure fixture reads the real =logicalCpuCount= from the first Scheme JSON
resource receipt and derives the rejected runnable count as
=logicalCpuCount + 1=.  It therefore validates the boundary against the
resolved machine capability rather than asserting a fixed 12-core host.  A
separate fixture proves that an unavailable runnable observation is rejected
before the first dependency or compile action.

This qualifies the per-sample admission mechanism.  It does not promote the
candidate GSLPH revision.  A fresh three-by-three formal comparison remains
required and must begin from the current POO Flow commit with a new output
directory.

The first fresh formal attempt then exposed an additional upstream capability
boundary before its first cold compile.  The resolved resource guard reported
=runnableProcessCountAvailable=false= and used zero only as its absence
sentinel.  Treating that sentinel as an observed quiet host would make the
comparison unverifiable.  The formal run was interrupted during dependency
seeding after 33.4 seconds, and the comparison gate rejects an unavailable
runnable observation explicitly.

gerbil-bazel PR 29 was squash-merged to =main= as
=ee8e621fb266019ef8d65422abf0dc8c5ddea422=.  That published commit closes
the upstream boundary.
The Scheme guard invokes =ps -axo state== directly for each admission, parses
the state column in Scheme, and represents observation availability separately
from the runnable count.  A successful zero-runnable observation is therefore
available, while deterministic failure injection remains unavailable even
when a CI count is also present.  Gerbil =__available-cores= remains the sole
CPU-capacity source.

POO Flow now pins that exact merged commit.  Its unavailable fixture uses the
explicit =GERBIL_BAZEL_GUARD_FORCE_RUNNABLE_UNAVAILABLE=1= contract instead of
overloading zero as a sentinel.  The downstream
=//scheme:compile_performance_audit_test= replay passed one of one targets in
30.1 seconds.  Bazel reported 170.893 seconds overall because changing the
external repository commit invalidated and rebuilt three dependent Gerbil
package actions before the test; the test critical path itself remained
bounded by the unchanged 60-second timeout.

After the squash merge, Bazel resolved the merged =main= commit and reused 38
content-identical action-cache entries.  The cached target remained green, but
an explicit =--nocache_test_results= replay reached the fixed 60-second timeout
before the synthetic suite completed.  A simultaneous process snapshot showed
multiple active Rust compiler processes and many concurrent ASP processes.
This is a host-contended validation attempt, not a qualified merged-pin replay.
The timeout must not be raised; an uncached pass under an admitted host remains
required before the merged pin is promoted.

The fresh-observation prerequisite is now qualified, so a new three-by-three
formal comparison may start.  This focused replay does not itself promote the
candidate GSLPH revision or supply a formal performance result.

The new =r3= formal comparison then exercised that boundary successfully.  Its
first baseline admission observed 51 runnable processes against 12 logical
CPUs, wrote =runnableProcessCountAvailable=true=, and stopped after the
413-millisecond resource-guard action with
=runnable-pressure-exceeds-logical-capacity=.  It produced
=host-admission.json= and =failure.context.json= before dependency seeding or
cold compilation began.  A CPU-sorted process snapshot confirmed material
concurrent work, including an active Rust compiler, multiple ASP processes,
multiple devenv processes, and Nix garbage collection.  This is a qualified
host-pressure rejection, not a baseline or candidate performance sample.
Repeating the formal comparison while that pressure remains would invalidate
the attribution policy.

* POO Flow root-compile regression correction

The merged gerbil-bazel PR 29 remains the owner of fresh host observation,
package-action execution, toolchain capability, and publish-pin replay.  It
does not choose the worker count inside POO Flow's own =build.ss= supervisor.
The two boundaries must therefore be measured separately.

The last green =main= run on the same GitHub =macos-26= lane completed
=//scheme:compile= in 356 seconds.  PR 12 reached the outer 900-second timeout
in the same action.  Its newly introduced default divided the absolute RSS cap
by the configured worker count.  Because that absolute cap includes the
supervisor baseline RSS, the formula systematically reduced an admitted
=N=-worker host to at most =N - 1= workers.  That local POO Flow admission
policy, rather than the merged gerbil-bazel package-action implementation,
owned the regression.

The corrected policy combines a project safety ceiling with two
machine-derived limits:

- the absolute process cap is the smaller of
  =baseline RSS + live allocatable memory= and the physical-memory-bounded
  machine cap;
- the 768 MiB minimum viable project RSS is distinct from the default 2 GiB
  per-worker reservation, with =POO_FLOW_BUILD_MEMORY_PER_WORKER_BYTES=
  retained only as an upward machine-calibration override.

The former fixed 5632 MiB ceiling was not a valid machine-independent safety
invariant.  On the current 32 GiB host it rejected the same whole-project
compile at both three workers and two workers, even though more than 20 GiB was
available and the separate system headroom remained intact.  Reducing workers
again would trade away throughput without proving that shared compile memory
fits the fixed ceiling.

The project cap is now machine-adaptive but still bounded.  Its raw project
ceiling is the greater of the 5632 MiB project minimum and one quarter of
physical memory, but the effective machine ceiling is also capped by physical
memory itself.  This matters on hosts smaller than the project minimum: the
guard must reject insufficient capacity, not advertise memory that the host
does not possess.  Live allocatable memory and baseline RSS can only lower the
admitted cap.  The 768 MiB value remains the minimum viable project RSS, while
a distinct 2 GiB per-worker reservation controls concurrency.  An environment
request may raise that reservation but cannot lower the safe default.  Worker
count remains derived from configured CPU count, admitted memory, reservation,
and the hard worker ceiling; no fixed =GERBIL_BUILD_CORES= value is introduced.

Memory observations are one evidence envelope, not independent trusted knobs.
An available-memory observation or override greater than physical memory, or a
baseline RSS greater than physical memory, is contradictory evidence and MUST
fail closed with a typed admission reason.  The guard MUST NOT silently clamp
such evidence and then publish =ready=.

#+begin_src typst :file ../../../.data/156-project-compile-machine-admission.svg
#set page(width: auto, height: auto, margin: 8pt)
#table(
  columns: (auto, auto),
  inset: 6pt,
  align: (left, left),
  [Quantity], [Definition],
  [Raw project ceiling], [$C_r = max(5632 "MiB", M / 4)$],
  [Machine ceiling], [$C_m = min(M, C_r)$],
  [Evidence consistency], [$E = (A_v <= M) and (B <= M)$],
  [Adaptive RSS cap], [$C = min(C_m, B + max(768 "MiB", A))$],
  [Effective worker reservation], [$R = max(2048 "MiB", R_r)$],
  [Admitted workers], [$W = min(W_c, floor(A_a / R), 3)$],
  [Ready premise], [$"Ready" => E and C <= M$],
  [Safety premise], [$P_w <= R => W P_w <= A_a$],
)
#+end_src

#+begin_src mermaid
flowchart LR
  SM["Physical system memory"] -->|"one-quarter machine share"| Q["Physical memory / 4"]
  PM["5632 MiB project minimum"] -->|"lower bound"| RC["Raw project ceiling"]
  Q -->|"scaling candidate"| RC
  SM -->|"hard physical upper bound"| MC["Machine RSS ceiling"]
  RC -->|"min with physical memory"| MC
  AV["Live available memory"] -->|"subtract system headroom"| AL["Allocatable memory"]
  BR["Baseline project RSS"] --> AC["Adaptive project cap"]
  AV -->|"available <= physical"| EC["Evidence consistency gate"]
  BR -->|"baseline <= physical"| EC
  SM --> EC
  AL -->|"live upper bound"| AC
  MC -->|"machine upper bound"| AC
  EC -->|"consistent"| AC
  EC -.->|"contradictory"| BAD["typed blocked-host-pressure"]
  RR["Requested worker reservation"] -->|"cannot lower 2048 MiB"| ER["Effective reservation"]
  AC -->|"admitted memory"| WC["Derived worker capacity"]
  ER -->|"floor admitted / reservation"| WC
  FC["Fixed 5632 MiB cap"] -.->|"counterexample: safe host compile rejected"| REJ["rss-limit-exceeded"]
  WC -->|"bounded by CPU and hard worker ceiling"| RUN["Building Framework compile"]
#+end_src

- [X] Minimum viable project RSS and per-worker peak reservation are distinct
  quantities.
- [X] A low environment override cannot weaken the safe worker reservation.
- [X] The machine ceiling scales to 8192 MiB on a 32 GiB host and remains 5632
  MiB on an 8 GiB host.
- [X] The effective machine ceiling never exceeds physical memory; a 1 GiB
  host cannot advertise the 5632 MiB project minimum.
- [X] Available-memory or baseline-RSS evidence above physical memory fails
  closed with a typed reason instead of producing =ready=.
- [X] Live allocatable memory and baseline RSS remain admission bounds.
- [X] Worker count remains resource-derived rather than fixed by CI policy.

=ProjectCompileWorkerAdmissionSafetyModel= records the missing proof premises.
Reservation arithmetic alone does not imply peak safety when actual
per-worker peak exceeds the declared reservation; the concrete countermodel
admits three 768 MiB reservations while exceeding the same envelope at a 1700
MiB peak.  =reservationUpperBoundsPeak= proves the corrected implication when
the effective reservation is a real peak upper bound.  A second concrete
countermodel shows that the legacy guard admitted contradictory observations
(1 GiB physical, 4 GiB available, 2 GiB baseline) and published a 5632 MiB
envelope.  =machineHardRssCapNeverExceedsPhysical= and
=guardEnvelopeNeverExceedsPhysical= prove the corrected bound, while explicit
evidence-consistency reasons reject the contradictory observation before
execution.  =contradictoryOverrideEvidenceHasExactTypedReasons= fixes the
reason identity and order, and =admittedMemoryEvidenceIsConsistent= proves that
an empty evidence-reason list implies both observation bounds.  The model also
proves that a low environment request cannot lower the safe default and that
the bounded cap maps 8 GiB to 5632 MiB and 32 GiB to 8192 MiB.

- Worker-admission Model AXLE: status =accepted=; 14 roots; 36 declarations; 0
  failed declarations; closure digest
  =sha256:11f0a1da908ea6a319442c8eae1c43885893e7b21297471b5b9b0c04d3176f8a=;
  source digest
  =sha256:5b41bb662670fdfaf3c5808bc4d9cbcd920a1075fc5560289d76b5f2ecdcd100=;
  exact bundle digest
  =sha256:9c6c146c23d79c125a44e1313451d21600008a33f5f00be19347d479da9645d5=;
  canonical source digest
  =sha256:043ee5f20414277a5cb57830540bde819fe7a2d3a37a10a4b27875e496ca2e93=;
  independent bundle digest
  =sha256:5055b1b99fc11e492f141fee5477a2b1ba156e4f02e74348056a6a77514bcb90=;
  source declarations =26=; proof-base imports/declarations =0/0=; roundtrip
  and theorem proof status =accepted=.
- Focused Lean: 2 jobs completed with no Lean warnings or errors.
- Focused Scheme guard target =//scheme:build_api_project_compile_guard_test=:
  1 Bazel test, 16 cases, 101 checks, all successful.
- Focused Recipe target completed the whole project compile under the adaptive
  cap and then passed 1 Bazel test, 9 cases, and 38 checks.

#+begin_src mermaid
flowchart LR
  GB["gerbil-bazel PR 29"]
  Package["Package actions and fresh host observation"]
  Root["POO Flow build.ss supervisor"]
  Physical["Physical memory"]
  Quarter["Physical memory / 4"]
  Floor["5632 MiB project minimum"]
  Ceiling["Machine ceiling = max(Floor, Quarter)"]
  Live["Baseline RSS + live allocatable memory"]
  Cap["Admitted RSS cap = min(Ceiling, Live)"]
  Reservation["Worker reservation = max(2 GiB, requested)"]
  Workers["Admitted worker count"]
  Compile["POO Flow root compile"]

  GB --> Package
  Package --> Root
  Physical --> Quarter
  Quarter --> Ceiling
  Floor --> Ceiling
  Ceiling --> Cap
  Live --> Cap
  Cap --> Root
  Reservation --> Workers
  Root --> Workers
  Workers --> Compile
#+end_src

The focused native Scheme guard suite completed with =All tests OK=.  The
declarative =//scheme:compile_performance_audit_test= passed one of one targets
in 51.5 seconds, and the runtime-WASM producer target completed successfully in
37.235 seconds.

An intermediate local =//scheme:compile= completed in 528.264 seconds of Bazel
elapsed time with a 510.24-second critical path.  Its native Scheme receipt
reported:

#+begin_example
configured-worker-count = 12
worker-count = 12
memory-per-worker-bytes = 805306368
memory-worker-capacity = 21
peak-rss-bytes = 4259545088
max-rss-bytes = 18226571509
elapsed-ms = 425634
#+end_example

The runtime stage compiled 381 modules and consumed 415.104 seconds.  This
receipt proved restored worker admission, but its 18.23 GB effective cap was
rejected because host availability had been mistaken for the project safety
limit.  It is not promotion evidence.  The later fixed-5632-MiB criterion was
also refuted: it killed the current compile at both three and two workers while
system headroom remained available.  Replacement evidence must instead bind
the machine-derived ceiling, live allocatable memory, baseline RSS, effective
worker reservation, derived worker count, observed peak, and terminal outcome.

The replacement focused compile completed under the machine-adaptive guard and
published the following native =POO_FLOW_PROJECT_BUILD_RECEIPT=.  For this
32-GiB host, the versioned formula derives an 8-GiB ceiling; the observed peak
remained below that ceiling and the action reached =completed=.

This paragraph and the following receipt are retained as historical evidence,
not as the current policy.  RFC 0147's reactive-memory correction rejects its
=memory-per-worker-bytes= and =memory-worker-capacity= fields as scheduler
inputs: the guard must detect an abnormal RSS trajectory and must not lower
normal =std/make= parallelism before execution.

#+begin_example
{
  "admission-outcome": "ready",
  "admitted-memory-bytes": 7767228416,
  "allocatable-memory-bytes": 19499151523,
  "available-memory-bytes": 21646635171,
  "baseline-rss-bytes": 822706176,
  "configured-worker-count": 12,
  "elapsed-ms": 315441,
  "max-rss-bytes": 8589934592,
  "memory-per-worker-bytes": 2147483648,
  "memory-worker-capacity": 3,
  "outcome": "completed",
  "peak-rss-bytes": 6030950400,
  "requested-max-rss-bytes": 8589934592,
  "rss-headroom-bytes": 2147483648,
  "source-identity": {
    "algorithm": "sha256",
    "digest": "c849f0e308c4480fb3d0f00f4b31e1ae80eb47df65ae892382e4145ce39b7bc9",
    "spec-count": 398,
    "stage-count": 3
  },
  "source-identity-materialization-elapsed-ms": 107,
  "system-memory-bytes": 34359738368,
  "worker-count": 3
}
#+end_example

The GitHub outer timeouts now pass a shorter timeout into the Scheme supervisor
through =POO_FLOW_BUILD_TOTAL_TIMEOUT_SECONDS=.  Linux uses 420 seconds inside
an 8-minute step, and macOS uses 840 seconds inside a 15-minute step.  This
leaves 60 seconds for the Scheme failure receipt, Bazel profile, and artifact
upload instead of allowing the GitHub runner to terminate the action without
evidence.

* PR 12 conflict closure receipt

The PR 12 merge candidate resolved all twelve Git conflicts and produced a
clean staged index.  The following validation receipts are recorded as
intermediate closure evidence:

- Bazel lock verification: PASS.
- Scheme unit test receipt: PASS, 1/1, elapsed 677.366 seconds.
- Scheme compile receipt: elapsed 389814 milliseconds, peak process-tree RSS
  4075716608 bytes, worker count 5 of 12, effective cap 5368709120 bytes.
- External Bazel full validation: PASS, elapsed 1116.100 seconds; the external
  root compile consumed approximately 668 seconds.

The reported peak RSS is still a process-tree RSS sum.  It is an operational
observation, not a proof of physical memory consumption, and its proximity to
the project anomaly threshold requires continued Scheme-side observability.
The guard must expose per-process attribution, sample timeline, process count,
and a non-double-counting memory metric before this concern can be closed.

The external cold path remains a regression.  Full external validation passed,
but its approximately 668-second root compile does not establish that the
cold-build performance regression has been resolved.  This receipt therefore
does not close the performance issue.

#+begin_src mermaid
flowchart LR
  C["PR 12 conflict resolution"] --> L["Bazel lock verification: PASS"]
  L --> U["Scheme unit test: PASS 1/1"]
  U --> R["Compile receipt: peak tree RSS 4.076 GB"]
  R --> G{"RSS near anomaly threshold?"}
  G -->|"yes"| O["Continue Scheme observability"]
  O --> A["Attribute process and sample timeline"]
  A --> M["Add PSS or platform footprint metric"]
  G -->|"no"| E["External full validation"]
  E --> X["PASS: 1116.100 s"]
  X --> P["Root compile about 668 s"]
  P --> Q["Cold-path regression remains open"]
#+end_src

* Linux proportional-memory enforcement correction

The first Linux CI run after the PR 12 conflict merge exposed a measurement
error in the Scheme process supervisor.  The child compiler was terminated
with exit code 70 after the guard summed 7,207,804,928 bytes of process-tree
RSS against the unchanged 5 GiB project ceiling.  Summing RSS across a process
tree counts shared pages once per process and is not a valid enforcement
metric for physical memory pressure.

The guard now reads =/proc/PID/smaps_rollup= directly in Scheme and sums PSS
for live members of the supervised process tree on Linux.  A process that
exits between the =ps= snapshot and the =/proc= read is ignored.  If a live
process has an unreadable rollup, its RSS is used conservatively for that
sample.  Other platforms retain process-tree RSS enforcement until they gain
an equivalent native proportional or footprint metric.

The 5 GiB safety ceiling is unchanged.  The receipt separates enforcement
from diagnostics through =memory-metric=, =peak-memory-bytes=, and
=max-memory-bytes=.  It retains =peak-rss-bytes= and =max-rss-bytes= for
schema compatibility, and adds largest-process RSS/PSS, peak process count,
and sample count for attribution.  Project receipts propagate the same fields
for blocked, failed, and completed outcomes.

#+begin_src mermaid
flowchart TD
  Tree["Supervised compiler process tree"] --> Snapshot["Scheme ps snapshot"]
  Snapshot --> Linux{"Linux smaps_rollup available?"}
  Linux -->|"yes"| PSS["Sum live-process PSS"]
  Linux -->|"no"| RSSFallback["Sum process-tree RSS fallback"]
  PSS --> Limit{"Guarded memory exceeds 5 GiB?"}
  RSSFallback --> Limit
  Snapshot --> Diagnostic["Retain tree RSS and largest-process attribution"]
  Limit -->|"yes"| Stop["Terminate child and emit terminal receipt"]
  Limit -->|"no"| Continue["Continue supervised build"]
  Diagnostic --> Receipt["Project and process guard JSON receipts"]
  Stop --> Receipt
  Continue --> Receipt
#+end_src

The isolated Darwin cold validation was stopped when the root
=//scheme:compile= action crossed twelve minutes.  Bazel reported 1,221.082
seconds total elapsed time, a 1,206.96-second critical path, and zero executed
tests.  This remains a failed historical cold-performance gate.  Its fixed
5 GiB promotion rule is superseded by the adaptive executor-memory correction
below; preserving that machine-sized constant would reproduce the CI defect.

* CI ownership correction for external validation

The first PSS-enabled CI attempt exposed duplicate ownership rather than a
second memory result.  The dedicated Linux external-module job and the native
Linux package job both executed the same =full= external consumer build before
the native job could reach its receipt-producing project compile.  Two
independent hosted runners received external shutdown signals during that
duplicated root action, so neither produced a Scheme terminal receipt.  The
logs contained no guard termination, compiler diagnostic, or memory-limit
receipt and therefore do not qualify as a PSS failure.

The dedicated external-module job remains the sole owner of the full consumer
compile.  Native Linux now performs only Bazel analysis of the external
consumer before proceeding to dependency validation and the supervised
=//scheme:compile= action.  This removes redundant work without weakening
coverage and ensures that the native job reaches the evidence-producing phase
within its existing budgets.

#+begin_src mermaid
flowchart LR
  Change["PR source change"] --> External["Dedicated Linux external job"]
  Change --> Native["Native Linux package job"]
  External --> Full["Full external consumer compile"]
  Full --> ConsumerReceipt["External compatibility receipt"]
  Native --> Analysis["External module analysis only"]
  Analysis --> Dependencies["Declared dependency packages"]
  Dependencies --> Supervised["Supervised //scheme:compile"]
  Supervised --> MemoryReceipt["Linux PSS and diagnostic RSS receipt"]
#+end_src

* Adaptive executor-memory correction (Gerbil-Bazel PR 33)

The subsequent Linux CI receipt proved that the 5 GiB boundary was not a
runner limit.  The failing worker reported =16,766,414,848 bytes= of system
memory and approximately =14.7 GiB= available, while the public
=gerbil_project_execution_policy= injected exactly =5,368,709,120 bytes=.  The
guard then summed per-process RSS, counted shared Gerbil and Gambit pages once
per compiler process, and terminated healthy four-core actions at
=5,940,273,152 bytes= and =5,489,696,768 bytes=.

The correction is owned entirely by merged =gerbil-bazel= PR 33 at the formal
=main= revision =7e1a55660502d0e2d67aa1e2fca66344ab2e7b9d=.  POO Flow only
pins that public revision.  The reusable policy now has these semantics:

1. The default =max_rss_bytes= is zero, selecting the live adaptive budget;
   zero is never unlimited memory.
2. Linux caps the host view with cgroup v2 =memory.max= and
   =memory.max - memory.current= before retaining headroom and selecting
   =GERBIL_BUILD_CORES=.
3. Linux process-tree enforcement sums PSS from =smaps_rollup= so shared pages
   are not duplicated.  A restricted or older procfs selects the explicit RSS
   fallback.
4. Receipt v1 remains stable.  Optional metric and cgroup fields expose which
   measurement and executor boundary were applied.

#+begin_src mermaid
flowchart TD
  Policy["Public project execution policy"] --> Adaptive["Default 0 means adaptive ceiling"]
  Host["Host total and available memory"] --> Effective["Effective executor memory"]
  Cgroup["cgroup v2 max and current"] --> Effective
  Effective --> Headroom["Retain adaptive headroom"]
  Adaptive --> Headroom
  Headroom --> Workers["Adaptive GERBIL_BUILD_CORES"]
  Workers --> Native["Native build.ss and std/make"]
  Native --> Probe{"smaps_rollup readable?"}
  Probe -->|"yes"| PSS["Process-tree PSS"]
  Probe -->|"no"| RSS["Process-tree RSS fallback"]
  PSS --> Receipt["Resource guard receipt v1"]
  RSS --> Receipt
#+end_src

The review-corrected upstream validation is green:

| Gate | Result | Duration |
|------+--------+----------|
| Local =gerbil-bazel= suite | 19/19 passed | =39.638 s= |
| Linux immutable-prebuilt CI | passed, including PSS and cgroup fixtures | =3m44s= |
| macOS Homebrew-host CI | passed with RSS fallback | =4m19s= |

The final POO Flow acceptance consumed the remote pinned revision without a
local module override.  =//scheme:compile= completed in =394.044 s= with 12
requested and 12 effective workers.  It recorded an adaptive ceiling of
=23,622,320,128 bytes=, peak process-tree RSS of =4,251,926,528 bytes=, and
=processTreeMemoryMetric=rss= on Darwin.  The cgroup fields were zero because
Darwin has no Linux cgroup boundary.  Final promotion remains the POO Flow PR
CI run pinned to the review-corrected upstream revision; no POO-owned memory
override or copied guard implementation is allowed.

* Runtime-WASM package workflow correction

The first merged Runtime-WASM workflow attempted to copy the generated
=bindings/runtime-wasm/workflows= directory before invoking the npm test
contract that creates it.  A clean checkout therefore failed immediately with
=No such file or directory= even though the Bazel Gerbil materialization had
completed successfully.

The npm =test= script already owns generation, the second deterministic
generation, the WASM build, and the Node tests.  The workflow now invokes that
single package contract and then packages the verified tree with
=npm pack --ignore-scripts=.  It no longer implements a second filesystem
determinism protocol around npm.

#+begin_src mermaid
flowchart LR
  Bazel["Bazel materializes Gerbil generator closure"]
  NpmTest["npm test"]
  Generate["Generate workflow sources"]
  Determinism["Repeat generation and verify determinism"]
  Wasm["Build and test WASM package"]
  Pack["npm pack --ignore-scripts"]
  Payload["Upload verified source-revision payload"]

  Bazel --> NpmTest
  NpmTest --> Generate
  Generate --> Determinism
  Determinism --> Wasm
  Wasm --> Pack
  Pack --> Payload
#+end_src

* GCC backend and incremental-build diagnosis (2026-09-02)

** Decision

GCC remains the production backend.  The comparison does not justify changing
POO Flow to Clang: the exact Clang cold run was still incomplete after
770.39 seconds (12.84 minutes), while the installed Gerbil 0.18.2 toolchain is
already configured for =gcc-16=, =g++-16=, single-host compilation, trusted C
tail calls, and the Darwin classic linker.  Gambit's upstream installation
guidance also recommends GCC because computed-goto support materially improves
generated-program runtime.

This is a compiler-policy decision, not an acceptance of the current build
latency.  Production keeps GCC and optimization; fast incremental development
must be solved through the canonical Gerbil module graph, stable per-project
dependency state, and ASP Build API profile ownership.

** Validation identity

- Host: Darwin arm64, 12 available logical CPUs, 32 GiB physical memory.
- Gerbil: 0.18.2, source revision =07c84815=.
- GCC configuration evidence: =gsc -v= reported =CC=gcc-16=,
  =CXX=g++-16=, =--enable-single-host=, =--enable-trust-c-tco=, and
  =LDFLAGS=-Wl,-ld_classic=.
- Bazel dependency owner: merged =gerbil-bazel= revision
  =7e1a55660502d0e2d67aa1e2fca66344ab2e7b9d=.
- Native entry point: canonical =gxpkg build= invoking the package =build.ss=
  and ASP Build API; no replacement shell scheduler was introduced.

** Timed receipts

Every duration has an explicit unit and terminal state.  Stopped runs are
censored observations, not successful baselines.

| Run | Terminal state | Elapsed seconds | Elapsed minutes | Interpretation |
|-
| Fresh Bazel downstream build | stopped after the root action exposed the same trajectory | 730.660 s | 12.18 min | critical path had already reached 715.80 s; not a successful cold baseline |
| Native GCC =gxpkg build -g= cold | build body completed; =/usr/bin/time -lp= later reported a sandboxed =sysctl kern.clockrate= wrapper error | 793.92 s | 13.23 min | exact native cold evidence |
| Native GCC =gxpkg build -g= warm | completed | 71.35 s | 1.19 min | warm state can be fast, but later runs proved it is not stable |
| Local exact-revision Clang cold comparison | stopped, incomplete | 770.39 s | 12.84 min | no compiler-switch benefit was demonstrated |
| Native GCC without =-g= | stopped, incomplete | 465.71 s | 7.76 min | removing debug information alone did not correct the trajectory |
| Native GCC with temporary =optimize: #f= cold | completed | 619.62 s | 10.33 min | 174.30 s, or 21.96 percent, faster than the completed GCC debug cold run; still unacceptable |
| Same temporary profile, first warm | completed | 126.14 s | 2.10 min | recompiled a non-empty module subset |
| Same temporary profile, second warm | completed | 101.25 s | 1.69 min | recompiled a different non-empty module subset |
| Explicit shared =framework-build-cache-root/build-deps= experiment | completed, rejected | 178.85 s | 2.98 min | slower warm result and an invalid cross-project cache owner |

The dependency actions also exposed a serial critical-path floor before the
root package began: =gerbil-utils= took about 156 seconds, =gerbil-poo= about
130 seconds, and =asp-gerbil-scheme= about 208 seconds, for approximately
494 seconds (8.23 minutes) on that observed dependency chain.  This is why a
POO Flow-only compiler flag cannot be the root fix.

** Corrected =build-deps= semantics

The temporary explicit cache experiment is rejected and was removed from
=build.ss=.  Local owner source at =.data/gerbil/src/std/make.ss= defines:

- =libdir= and =bindir= under =GERBIL_PATH= by default;
- =build-deps= as =(path-expand (or build-deps_ "build-deps") srcdir)=;
- =optimize: #t= and =parallelize: #t= as the upstream defaults.

Therefore =build-deps= is not, by default, a global file under
=GERBIL_PATH=.  Its correct default owner is the =srcdir= of each make
invocation.  The current ASP function named =framework-build-cache-root=
evaluated to the Gerbil installation root
=/opt/homebrew/Cellar/gerbil-scheme/0.18.2-source_1/v0.18.2/= on this host.
Using that value for =build-deps:= would merge unrelated projects into one
state location and contradict the upstream per-project requirement.

** Root-cause boundary

The completed =optimize: #f= run saved only 21.96 percent and still took more
than ten minutes.  The two subsequent warm runs rebuilt different module
subsets.  Together with the roughly 494-second dependency chain, this narrows
the remaining work to three owners:

1. ASP Build API must expose a small declarative development/production profile
   that lowers directly to native =std/make= settings.
2. The development profile must retain upstream per-project =srcdir/build-deps=
   semantics and prove stable no-op warm behavior before promotion.
3. Package topology and generated native units must be reduced or invalidated
   correctly; disabling GCC optimization globally is not an architectural fix.

POO Flow remains a downstream consumer.  It must not duplicate the scheduler,
invent a global cache, or own a second compiler policy.  Once the ASP owner API
is released, POO Flow should select the profile declaratively and pin that ASP
revision through =gerbil.pkg=.

#+begin_src mermaid
flowchart TD
    A[POO Flow build.ss] --> B[ASP Build API package profile]
    B --> C{Build intent}
    C -->|development| D[Native std/make incremental profile]
    C -->|production or CI release| E[GCC optimized production profile]
    D --> F[Per-project srcdir/build-deps]
    D --> G[GERBIL_BUILD_CORES from available CPUs]
    E --> H[Gerbil and Gambit GCC pipeline]
    F --> I[Stable no-op warm receipt]
    G --> I
    H --> J[Runtime and release gates]
    I --> K[Bazel consumes the same owner API]
    J --> K
    K --> L[POO Flow downstream receipt]
#+end_src

Promotion is blocked until the owner API demonstrates both a completed cold
build and at least two consecutive no-op warm builds.  An incomplete faster
trajectory, a compiler switch, or a shared installation-root cache is not a
promotion receipt.

** PR 12 downstream CI receipt

Commit =4cad868c= completed the full GitHub check set.  Durations below are
wall-clock job durations reported by GitHub Actions; all comparisons use the
immediately preceding green PR 12 run rather than a local estimate.

| Required check | Current duration | Previous duration | Delta | Result |
|-
| actionlint | 15 s | not performance-sensitive | n/a | passed |
| POO Flow Lean proof bridge | 27 s | not performance-sensitive | n/a | passed |
| POO Flow external Bazel module | 493 s (8 min 13 s) | no comparable value recorded here | n/a | passed |
| Linux Gerbil package | 760 s (12 min 40 s) | 811 s (13 min 31 s) | -51 s (-6.3 percent) | passed |
| macOS Gerbil package | 947 s (15 min 47 s) | 1590 s (26 min 30 s) | -643 s (-40.4 percent) | passed |
| Python runtime wheel | 39 s | downstream continuation | n/a | passed |
| Runtime WASM source-build | 1392 s (23 min 12 s) | 1664 s (27 min 44 s) | -272 s (-16.3 percent) | passed |

The three lock refresh/verify jobs and the package publication job were
conditionally skipped as designed; they are not failures.  The macOS and WASM
reductions are material, but the absolute durations remain too high.  This
receipt validates the merged =gerbil-bazel= revision as a downstream
improvement; it does not close the ASP Build API incremental-profile work.

#+begin_src mermaid
flowchart LR
    L0[Linux previous 811 s] -->|minus 51 s / 6.3 percent| L1[Linux current 760 s PASS]
    M0[macOS previous 1590 s] -->|minus 643 s / 40.4 percent| M1[macOS current 947 s PASS]
    W0[WASM previous 1664 s] -->|minus 272 s / 16.3 percent| W1[WASM current 1392 s PASS]
    L1 --> N[Next owner: ASP Build API profile]
    M1 --> N
    W1 --> N
#+end_src

** Runtime WASM timeout and stale-run cancellation

The Runtime WASM workflow now enforces cancellation at two independent
boundaries:

1. =concurrency.cancel-in-progress: true= cancels an older run for the same Git
   ref as soon as a newer commit starts.  An obsolete package build no longer
   consumes the macOS runner until its job timeout.
2. The existing 30-minute job deadline remains the final bound, while each
   measured slow step has its own deadline.

The step budgets are derived from the completed 23-minute-12-second source-build
receipt rather than fixed CPU assumptions:

| Step | Observed duration | Hard timeout | Margin |
|-
| Install Gerbil project dependencies | 343 s (5 min 43 s) | 480 s (8 min) | 137 s |
| Materialize POO Flow Gerbil project | 351 s (5 min 51 s) | 480 s (8 min) | 129 s |
| Build, test, and pack the package | 585 s (9 min 45 s) | 720 s (12 min) | 135 s |

A step deadline is a failed build receipt and blocks promotion.  A concurrency
cancellation means that the run was superseded by a newer commit; it must not be
reported as a test failure or success for the newer revision.

#+begin_src mermaid
flowchart TD
    P[Push on ref] --> C{Older WASM run active?}
    C -->|yes| X[Cancel superseded run]
    C -->|no| I[Install dependencies: 8 min max]
    X --> I
    I --> M[Materialize project: 8 min max]
    M --> B[Build test pack: 12 min max]
    B --> J{Whole job within 30 min?}
    J -->|yes| S[Publish green source-build receipt]
    J -->|no| F[Cancel and fail timeout receipt]
#+end_src
