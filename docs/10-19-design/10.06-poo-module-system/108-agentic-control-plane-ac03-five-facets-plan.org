#+title: AC-03 Five Typed Facets Implementation Plan

* Delivery

Perform a direct canonical cutover in the current checkout.  Do not retain the
flat Bundle constructor or schema.  Commit only after AC-01, AC-02, and AC-03
qualification passes; preserve unrelated build and CLI-support changes.

* Step 1 — Facet object family

Add POO-native constructors and predicates for Organization, Authority,
Context, Protocol, and Evidence facets, including named typed-empty
constructors.  Each facet exposes the common schema/name/entities/relations/
constraints contract.

* Step 2 — Bundle and canonical cutover

Replace the flat Bundle constructor with an epoch plus exactly five facets.
Derive the shared typed identity universe, normalize facets in fixed order, and
advance the canonical schema version.  Remove the old constructor route.

* Step 3 — Validation

Move existing AC-01 rules behind facet-local projections.  Add mandatory facet
shape checks, shared identity checks, and ordered cross-facet diagnostics for
Authority, Context, Protocol, and Evidence references.

* Step 4 — Fixture migration

Migrate AC-01 and AC-02 scenarios to five facets with explicit typed-empty
Protocol and Evidence values where appropriate.  Add focused AC-03 positive and
negative scenarios without compatibility fixtures.

* Step 5 — Closure

Compile semantic owners; run AC-01, AC-02, and AC-03 tests; parse RFC 107–109;
update the owner matrix and execution status; run =git diff --check=; then create
one path-scoped commit.
