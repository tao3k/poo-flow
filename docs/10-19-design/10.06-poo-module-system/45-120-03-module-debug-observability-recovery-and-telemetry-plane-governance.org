#+title: Module Debug, Observability, Recovery, and Telemetry Plane Governance
#+language: en

* Status

This RFC freezes the accepted Module debug and observability contract.  It
separates immutable semantic observation, debug and recovery control, and
effectful runtime telemetry.  It does not admit an implementation, a mutable
trace store, a public observability DSL, or a fifth required Module slot.

* Scope

This RFC owns:

- the three-plane Module observation and debugging model;
- immutable observation events and their generation lineage;
- dispatch, fixed-point, capability, and lifecycle observation responsibilities;
- continuable conditions and bounded recovery continuations;
- semantic and runtime performance-observation boundaries;
- redaction before telemetry publication; and
- core and Module-local file ownership.

It does not own:

- upstream POO dispatch, slot, prototype, or C3 implementation;
- the four-slot Module semantic contract;
- Type and Contract prototype construction;
- Module-local pure domain transformations;
- runtime handles, external telemetry sinks, or deployment policy; or
- a macro language for probes, events, handlers, or recovery clauses.

* Central decision

Module remains a higher-order POO value with exactly four required
responsibilities:

- =identity=;
- =imports=;
- =capabilities=; and
- =profiles=.

Debug and observability are derived from immutable evaluation generations.  They
are not a fifth Module slot, mutable Module state, or a side-effecting logger
embedded in semantic evaluation.

The architecture has three explicit planes:

#+begin_src mermaid
flowchart LR
  E["immutable Module evaluation generation"] -->|"projects source-owned semantic evidence"| O["semantic observation plane"]
  O -->|"signals an explainable condition when policy requires intervention"| D["debug and recovery control plane"]
  D -->|"returns an authorized recovery decision to the coordinator"| E
  O -->|"applies authority and redaction before publication"| T["runtime telemetry plane"]
  T -->|"publishes logs, metrics, traces, and runtime measurements through adapters"| S["external telemetry sinks"]
#+end_src

No runtime telemetry effect may flow back into semantic evaluation as hidden
state.

* Semantic observation plane

The semantic observation plane is pure and immutable.  It derives observation
from existing POO evaluation values, including:

- =DispatchEvaluation=;
- recursive Module import reachability evaluation;
- capability closure evaluation;
- EffectIntent projection; and
- =CapabilityMaterialization= lifecycle generations.

It does not duplicate those values in a second mutable trace model.

** ObservationEvent prototype

An ObservationEvent is a native POO value or prototype-derived value with three
required responsibilities:

| Responsibility | Meaning |
|----------------+---------|
| =source= | Stable identity of the source evaluation value. |
| =generation= | Immutable evaluation generation identity. |
| =evidence= | Source-owned semantic evidence referenced by the observation. |

The following responsibilities are derived:

- =lineage=;
- =phase=;
- =outcome=;
- =explanation=; and
- =redacted-projection=.

=evidence= is an admitted POO evidence value.  It is not a copied alist snapshot,
runtime handle, raw continuation, secret, or mutable log entry.

Observation kinds are expressed through prototype specialization.  They are not
maintained in a central string or symbol enumeration.

* Evaluation observation responsibilities

** Dispatch observation

Dispatch observation may derive:

- receiver and responsibility identities;
- instance, type, and prototype candidate sources;
- candidate enumeration and C3 precedence;
- the selected candidate and inherited =next= relation;
- dispatch budget consumption;
- cycle and cache identity;
- outcome, provenance, and explanation.

The inherited =next= or =super= relation is dispatch ancestry.  It is not a
recovery continuation.

** Fixed-point observation

Recursive Module import observation may derive:

- demand path and reachable import edges;
- node, edge, depth, and refinement counts;
- SCC membership;
- productive, suspended, unproductive, divergent, or denied cycle
  classification;
- bottom, refinement delta, join, convergence, and invalidation evidence;
- observation and evaluation budget consumption;
- semantic cache hit, miss, publication, and invalidation evidence; and
- the first generation at which progress stopped or divergence became
  observable.

These facts are semantic counters.  They do not read a clock or process metric.

** Capability observation

Capability closure observation may derive:

- requirement and provision identities;
- attenuation paths;
- withdrawal, denial, incompatibility, conflict, or absence evidence;
- contribution-admission priority distinct from C3 dispatch precedence;
- reachable closure provenance; and
- closure outcome and explanation.

Semantic capability satisfaction must not be presented as proof of runtime
authorization or resource availability.

** Lifecycle observation

Capability materialization observation may derive:

- immutable lifecycle generation and event lineage;
- authorization, claim, materialization, lease, handoff, revocation, cleanup,
  and reconciliation transitions;
- conditional lifecycle-head commit evidence;
- cancellation and crash-ambiguity evidence;
- logical exactly-once reconciliation outcome; and
- redacted runtime receipts.

Runtime handles remain in the adapter-owned handle store and never enter an
ObservationEvent.

* Debug and recovery control plane

The debug and recovery plane consumes semantic observations and decides whether
evaluation may continue, suspend, refine, invalidate, reconcile, or abort.

It is expressed with POO-native condition and policy prototypes.  It does not
add host-language clause syntax.

#+begin_src mermaid
flowchart LR
  A["cycle, budget, cache, condition, or performance anomaly"] -->|"constructs an immutable ObservationEvent"| O["semantic observation"]
  O -->|"signals a continuable condition with explanation"| H["debug coordinator handler"]
  H -->|"selects continue, suspend, refine-budget, invalidate-cache, reconcile, or abort"| R["RecoveryDecision"]
  R -->|"authorizes one bounded resumption"| C["evaluation coordinator"]
  C -->|"creates the next immutable generation"| N["resumed evaluation"]
#+end_src

** Condition boundary

A condition contains immutable semantic evidence and available recovery
choices.  It does not contain:

- a raw continuation;
- a runtime handle;
- a secret;
- a mutable trace buffer; or
- an effectful callback.

Conditions must be observable before a recovery choice is executed.

** RecoveryDecision boundary

RecoveryDecision is a POO value or prototype-derived value.  Decision variants
are prototype specializations rather than a mutable command tag.

A RecoveryDecision may authorize:

- continue with unchanged policy;
- suspend and retain the immutable evaluation generation;
- refine an explicit budget;
- invalidate an identified semantic cache entry;
- reconcile an ambiguous runtime transition;
- deny further evaluation; or
- abort the evaluation.

The decision is pure.  The evaluation or runtime coordinator owns execution.

* Continuation and resumption separation

Three continuation concepts remain distinct:

| Concept | Owner | Meaning |
|---------+-------+---------|
| POO inherited continuation | Upstream POO dispatch | Continue through C3 ancestry with =next= or =super=. |
| Fixed-point resumption | Evaluation coordinator | Continue refinement from an immutable generation. |
| Recovery continuation | Debug coordinator | Resume control suspended by a continuable condition. |

First-class continuations and =call/cc= may be used only inside the admitted
debug coordinator.  A raw continuation is ephemeral and coordinator-owned.  It
must not be serialized, cached, logged, returned through the public interface,
stored in Module, or included in a receipt.

A stable resumption identity may appear in redacted evidence, but the authority
and raw continuation remain inside the coordinator.

* Performance observation boundary

Semantic performance observation is deterministic for equal admitted inputs and
evaluation policy.  It may include:

- candidate, dispatch, node, edge, and depth counts;
- SCC and refinement counts;
- cache hit, miss, invalidation, and recomputation counts;
- budget consumption; and
- generation-to-generation progress deltas.

Runtime performance telemetry may include:

- monotonic duration;
- CPU time;
- process RSS or platform footprint;
- I/O latency;
- adapter queue delay; and
- external sink publication latency.

Wall-clock, CPU, memory, and I/O measurements are effects.  They belong to the
runtime telemetry adapter and must not be read by =func.ss= or semantic
observation functions.

An abnormal runtime threshold is a diagnostic signal.  It must lead to
process-and-phase attribution rather than merely raising the threshold.

** Development memory diagnosis and final-defense boundary

Development memory safety is a layered protocol, not one guard process:

#+begin_export mermaid
flowchart LR
  S["source-shaped POO slot bindings"] -->|"detect self-slot initializer before realization"| A["authoring observation"]
  A -->|"admits owner-named operation"| D["debug memory span and checkpoints"]
  D -->|"compare native POO policy and heap samples"| R["bounded memory receipt"]
  R -. "diagnostic evidence only; no hidden semantic input" .-> O["semantic observation and developer output"]
  H["launch-time Gambit heap ceiling"] -->|"bounds source import before debug can load"| D
  G["outer process-tree anomaly guard"] -. "final defense for compiler children and pre-import failure" .-> H
#+end_export

The source-shaped authoring observation owns the first and cheapest diagnosis.
In an upstream =.o= body, a bare right-hand identifier equal to its slot name
is self dispatch.  For example, =policy: policy= does not capture a same-named
constructor argument; it constructs a recursively computed =self.policy= slot.
This failure can allocate until heap exhaustion before an ordinary =.ref=
returns.  Constructors bind a distinct lexical identifier such as
=policy-value= before entering =.o=.

This applies to both colon and parenthesized POO clauses.  The killed =gxtest=
root cause was =(project-id project-id)= in
=poo-flow-session-registry-receipt=.  Object construction remained lazy and
appeared to succeed; the first =.ref project-id= during session graph
serialization recursively dispatched the same slot until Gambit failed in
=hash-ensure-ref= or =make-vector=.  The constructor now captures every input
under a distinct =*-value= name before entering =.o=.

The in-band debug layer owns native POO =MemoryPolicy=, =MemorySample=, and
=MemoryReceipt= values.  Pure comparison derives a verdict from explicit
samples.  The effectful adapter alone may read Gambit's heap counters, request
an explicitly configured collection, emit a bounded aggregate through upstream
DDT, or signal a typed fail-closed anomaly.  Long operations expose
owner-named checkpoints rather than one anonymous end-of-process peak.
The native monitor variant runs a development operation in a Scheme thread and
samples from the owner thread at the policy interval.  On a fail-closed receipt
it terminates that debug worker before raising the typed anomaly.  This covers
yielding lazy-slot cycles that never return to an ordinary span checkpoint.

The debug module cannot protect the memory used to load itself.  A native test
or build launcher therefore applies a Gambit maximum heap before source import.
That ceiling is a final safety property, not a diagnosis and not permission to
widen a budget until a test passes.  The pre-existing process-tree anomaly
guard is reclassified as the outermost final defense for compiler children and
pre-import failures.  It must not own semantic reason codes, phase identity, or
normal POO debug policy.

A shell poller or shell test wrapper is not an observability implementation.
It cannot see POO phase identity, native contracts, or lazy slot realization,
so this architecture must not add one as the memory detector.  The normal
diagnostic path is Scheme-native: source-shaped observation followed by native
POO memory spans/checkpoints.  Gambit's own launch ceiling and the existing
external process-tree guard remain independent final defenses only.

Daily development uses the native Gerbil build and isolated gxtest route.  A
source-first run is a deliberate cold graph/expansion diagnostic: it keeps the
source module graph resident and therefore has a different baseline from an
installed-library test.  A source-first heap failure is not by itself evidence
that the tested runtime operation leaks; the failing phase and in-band receipt
must agree before making that claim.

* Runtime telemetry plane

The runtime telemetry plane consumes redacted ObservationEvent projections and
runtime-owned measurements.  It may publish logs, metrics, or traces through an
adapter.

Before publication, the adapter must:

- apply observer authority;
- remove secrets and raw handles;
- remove raw continuations and recovery authority;
- preserve stable semantic identity and generation lineage;
- distinguish semantic evidence from runtime measurement; and
- emit a publication receipt or actionable denial.

Telemetry failure must not silently mutate or invalidate semantic evaluation.

* File and dependency ownership

The core owners are:

| Owner | Responsibility |
|-------+----------------|
| =src/module-system/observability.ss= | Pure ObservationEvent construction, projection, lineage, and explanation. |
| =src/module-system/debug.ss= | Condition, RecoveryDecision, coordinator, and bounded resumption policy. |
| Runtime observability adapter | Redaction, runtime measurement, publication, and receipt effects. |

The existing =src/module-system/observability.ss= is implementation evidence
for presentation-trace projections.  It does not by itself admit the POO-native
ObservationEvent and three-plane contract defined here.  This new architecture
makes no compatibility or migration commitment to presentation alists.

The target =src/module-system/debug.ss= is not implementation-admitted.

** Module-local owners

Every Module receives the core generic observation of its four responsibilities
and evaluation generations.

A Module adds local =observability.ss= only when it introduces domain-specific
observation prototypes or projections.  It adds local =debug.ss= only when it
introduces domain-specific conditions or recovery policy.  Empty placeholder
files are rejected.

#+begin_src mermaid
flowchart TB
  T["Module-local types.ss"] -->|"contracts events, conditions, and decisions"| F["Module-local func.ss"]
  F -->|"supplies pure domain projections"| O["Module-local objects.ss"]
  O -->|"supplies evaluation identities and responsibilities"| V["optional Module-local observability.ss"]
  V -->|"signals typed conditions without effects"| D["optional Module-local debug.ss"]
  V -->|"projects only redaction-safe explanations"| I["Module-local interface.ss"]
  V -->|"hands redacted events across the effect boundary"| R["runtime telemetry adapter"]
#+end_src

=observability.ss= and =debug.ss= must not be imported by =types.ss=,
=func.ss=, or Module-local =objects.ss=.  Runtime telemetry owners may consume
semantic projections; semantic owners never import runtime telemetry code.

* Public interface

This RFC adds no public macro, probe clause, handler clause, event registry, or
trace DSL.

The ordinary user interface may request a redaction-safe explanation through an
already admitted POO responsibility.  It cannot receive:

- raw candidates hidden by authority;
- secrets or runtime handles;
- raw continuations;
- coordinator recovery authority; or
- mutable trace storage.

* Ownership

- Upstream =gerbil-poo= owns prototype, dispatch, C3, and inherited continuation
  mechanics.
- =poo-flow-core= owns ObservationEvent, condition, RecoveryDecision, semantic
  budgets, and coordinator contracts.
- Each domain Module owns only its domain-specific observation and debug
  specializations.
- Runtime adapters own clocks, process metrics, external sinks, handles,
  redaction execution, and publication receipts.
- =wendao-episteme= may extend observation and debug prototypes for its domain
  without owning the generic core.
- GSLPH, Gerbil-Bazel, Nix, and =gerbil.pkg= own no Module observability
  semantics.

* Admission checklist

** Semantic governance

- [X] Keep Module at exactly four required responsibilities.
- [X] Derive observation from immutable evaluation generations.
- [X] Define ObservationEvent as a native POO value with source, generation, and
  evidence responsibilities.
- [X] Keep dispatch, fixed-point, capability, and lifecycle evidence
  distinguishable.
- [X] Keep semantic observation, debug control, and runtime telemetry separate.
- [X] Keep =super=, fixed-point resumption, and recovery continuation distinct.
- [X] Confine raw =call/cc= continuations to the debug coordinator.
- [X] Separate deterministic semantic counters from runtime measurements.
- [X] Redact before effectful telemetry publication.
- [X] Add no mutable trace store, public DSL, or empty Module-local files.

** Implementation admission

- [ ] Admit the shared =types.ss= foundation.
- [ ] Admit POO-native ObservationEvent construction and projection.
- [ ] Admit typed conditions and RecoveryDecision prototypes.
- [ ] Admit bounded coordinator continuation and resumption tests.
- [ ] Admit fixed-point cycle, budget, cache, and progress observations.
- [ ] Admit runtime metric attribution and redacted telemetry receipts.
- [ ] Select one Module with a real domain-specific observation or condition as
  the first local exemplar.

* Rejected alternatives

- A fifth =observability= slot on Module.
- A mutable global trace store.
- Copying evaluation state into alist event snapshots.
- One empty =observability.ss= or =debug.ss= in every Module.
- Boolean-only failure reports with no evidence or explanation.
- Treating =super= as a recovery continuation.
- Serializing or logging raw =call/cc= continuations.
- Reading clocks, RSS, or I/O state in semantic functions.
- Raising a runtime threshold without process-and-phase attribution.
- Publishing secrets, handles, or recovery authority through telemetry.
- A public probe, handler, trace, or recovery clause DSL.

* Successor contract

The first implementation slice remains the shared POO-native =types.ss=
foundation.  ObservationEvent and RecoveryDecision require that foundation
before any coordinator or telemetry work can be implementation-admitted.

The first Module-local exemplar must introduce a real domain-specific
observation or continuable condition.  A synthetic example or empty owner does
not satisfy admission.

* Accepted upstream ownership correction

POO slot and inheritance trace projection belongs to the pinned
=gerbil-poo= dependency.  Project observability MUST reuse
=gerbil-poo/debug.ss= for native POO trace projection rather than
implementing a parallel object tracer.

POO Flow owns the domain semantics above that projection:

- immutable semantic observation events derived from evaluation
  generations;
- explicit debug and recovery decisions, including bounded
  continuation ownership;
- redacted runtime telemetry publication behind an effect boundary.

These responsibilities do not add an observability slot to Module.
Module remains exactly =identity=, =imports=, =capabilities=, and
=profiles=.  Upstream trace projection, semantic observation, recovery
control, and runtime telemetry are distinct ownership planes.

#+begin_src mermaid
flowchart TD
  D["Pinned gerbil-poo/debug.ss"] -->|"projects native POO slots and inheritance traces"| P["Trace projection"]
  G["Immutable evaluation generation"] -->|"emits evidence without semantic mutation"| E["ObservationEvent POO value"]
  P -->|"enriches explanation without becoming Module state"| E
  E -->|"offers explicit bounded recovery choices"| R["Debug and recovery coordinator"]
  E -->|"passes through mandatory redaction"| X["Runtime telemetry boundary"]
  X -->|"publishes clocks, CPU, RSS, and I/O as effects"| S["Telemetry sink"]
  M["Module with four responsibility slots"] -->|"is observed externally without a fifth slot"| E
  S -.->|"never feeds hidden runtime state back into semantic evaluation"| M
#+end_src

* Implementation admission checklist

** Native development-debug consumption slice

The first executable debug slice is
=t/gerbil-poo-debug-admission-test.ss=, registered as
=//scheme:gerbil_poo_debug_admission_test=. It directly consumes upstream
=DDT=, =trace-poo=, and =traced-function=; it introduces no project tracer,
public debug macro, runtime sink, or recovery coordinator.

The source identities inspected for this slice are:

| Package revision | File | Git blob |
|------------------+------+----------|
| =gerbil-poo@1c3225637525400341a09e958296962c10184ead= | =debug.ss= | =8cfdaf1458c57d933629368f7d20f40c4feceac5= |
| =gerbil-utils@f45a4ef3bfecd2af39e114ed736ce9082cbb8244= | =debug.ss= | =ad7d4a45cb4a756ab5466b773c97a114a6d626e6= |

The current local source blobs match these pinned blobs. This is source
identity evidence, not a claim that a cached compiled dependency has been
rebuilt from those revisions or that the Bazel suite has passed.

Development-debug consumers MUST observe these constraints:

- =DDT= may display values using an explicit native Type's =.sexp<-=.
  It is diagnostic printing, not Contract admission or semantic evidence.
- =trace-poo= derives a new receiver and wraps inherited procedure-valued
  slots. It is not transparent instrumentation of the original receiver or
  its cache. A debug variant MUST NOT replace a production cache identity.
- Supply an explicit non-sensitive trace name. The default name projects
  the object's =sexp= responsibility and can evaluate user-defined code.
- Creating a trace variant must not force lazy import contributions. The
  test exercises the actual four-responsibility SemanticModule constructor,
  rather than adding a fifth debug slot or an empty local debug owner.
- Function tracing reports normal call/return values. An exception propagates
  without a normal return line; logs are not a complete outcome protocol.
- Traced arguments and returns are raw output. =DDT= type failures and
  conversion failures can print the raw candidate. A conversion callback
  alone is therefore NOT a fail-closed redactor.
- Only synthetic/non-sensitive values may enter this development output.
  Production telemetry requires a separate authority/redaction boundary;
  capturing a string port does not perform redaction.
- Binding mutation through =trace!= and process-wide thread tracing hooks
  remain outside this slice. Semantic =types.ss=, =func.ss=, and
  =objects.ss= do not import these debug facilities.

Native validation uses the captured project environment and cached toolchain:

#+begin_src sh
./.devenv/devenv-profile-exec gxtest t/gerbil-poo-debug-admission-test.ss
#+end_src

This bounded slice does not admit ObservationEvent, RecoveryDecision,
fixed-point cycle evidence, runtime telemetry, or recovery resumption.

The native debug test passed seven cases and 33 checks with exit code 0 on
2026-09-08. Buildifier validation passed for the registered test targets.
This is a focused native receipt, not full-suite or Bazel acceptance.

** Framework delivery path

The intended product is a usable upstream-based POO observability framework,
not only a collection of tracing tests. The first end-to-end domain is the
actual SemanticModule Contract admission result, including successful
admission, missing responsibilities, invalid responsibility values, and
native ancestry rejection.

Implementation must connect these responsibilities without conflating them:

1. A native observation prototype preserves source-owned admission evidence,
   subject identity, generation, causal evidence, producer provenance, and
   disclosure classification. Explicit =define-type= declarations own its
   Contracts; existing strict presentation structs are not promoted into it.
2. A pure diagnostic view explains the same evidence and supports bounded
   inspection of responsibility failures. It must not re-evaluate the subject
   under inspection or invent missing generation/provenance facts.
3. An explicit development adapter uses upstream =DDT= and =trace-poo= for
   non-sensitive diagnostic projections and isolated debug variants. It never
   parses printed traces into authoritative evidence or replaces production
   receiver/cache identity with the variant's identity.
4. Native prototype refinement supplies domain-specific explanation behavior.
   New observation domains must not require adding a central tag switch.
5. An example must exercise one real Module admission failure through evidence,
   explanation, and captured upstream debug output. Negative tests must retain
   the first cause, reject forged evidence, avoid forcing lazy imports, and
   show that omitted/private fields never reach the safe diagnostic output.

This first framework path adds no recovery execution, remote telemetry sink,
clock-based semantic input, global hook, or fifth Module slot. Such extensions
retain their separate authority and admission gates. Concrete exported API
names and the semantic source owner remain subject to the owner-discovery
gate in RFC 45-120-15; this section does not invent their implementation path.

On 2026-09-08 the normal ASP Search command could not supply that gate:

#+begin_src sh
./.devenv/devenv-profile-exec asp search playbook --languages gerbil-scheme --rg 'poo-flow-classification-evidence|poo-flow-module-observation' src/module-system/types.ss src/module-system/observability.ss
#+end_src

The sandboxed call returned =host-operation-not-permitted=. The exact command
with the required Host permission reached Runtime but exited 2 with
=topology-manifest-unavailable= for =.agents/asp/topology/manifest.org=.
Separately, =asp gerbil-scheme info= reported an unadmitted Runtime Server
route. No parser-owned source evidence was produced. Repairing the registered
workspace topology and confirming the normal parser route precede semantic
owner implementation; grep matches and upstream unit tests cannot substitute
for that evidence.

** Current native framework slice

The user subsequently authorized temporary =rg= / =fd= owner exploration.
That exploration located the existing native =src/observability/= package,
including its Diagnostic/Receipt Contracts and JSON Schema consumers. The
framework now extends that owner as documented in RFC 45-120-15; no parallel
tracer or strict-struct replacement is introduced. The parser checks below
remain unclosed rather than being inferred from local text discovery.

The concrete pure entry points are
=poo-flow-observation-identity=, =poo-flow-observation-provenance=,
=poo-flow-observation-context=, =poo-flow-observe-contract-admission=,
=poo-flow-observe-admission-evidence=, =poo-flow-observation-explain=, and
=poo-flow-observation-summary=. They operate on native POO values.
=poo-flow-observation-debug= is a separately imported development-output
adapter; it hands only freshly projected aggregate facts to upstream debug.

The observation preserves explicit source, generation, causal references, and
provenance with bounded structured admission facts. Producer correlation is
not cryptographic authentication. Detail truncation/repeated evidence affects
observation completeness, never the original Contract verdict. Candidate,
raw context, diagnostic text, and runtime handles are not retained in the
projected facts. The source Contract is not re-run during explanation.

The framework adds no public =inspect/explain/diff/simulate= DebugDemand
evaluator, authorization policy, recovery coordinator, production sink, or
implicit global tracing hook. In particular, counts are not automatically
public data and the development output is not a production redaction policy.

The separately imported debug adapter now includes native POO memory policy,
sample, and receipt values plus explicit checkpoint/span effects and a native
Scheme monitor.  Its first regression found and repaired a real =.o= self-slot
initializer cycle in the receipt constructor itself.  The framework test now
covers pure comparison, bounded DDT projection, typed fail-closed receipts,
worker termination, and containment of an actual non-returning lazy POO
self-slot cycle.  The existing module-system authoring observation covers the
source-shaped pre-realization form.

The executable example is =tools/observability-admission-demo.ss=. The focused
gate is =//scheme:observability_framework_test=; existing feedback, strict
presentation, shared foundation, native debug, and Module tests are retained.
See =src/observability/README.org= for the exact commands and native extension
contract.

The focused native command recorded there passed six suites and 174 checks
with exit code 0 on 2026-09-08. The standalone example also exited 0 and showed
=(failure-path (imports) cause prototype-mismatch)= followed by the upstream
call/return trace and typed aggregate output. This evidence admits the bounded
development path, not a full parser/pinned-build/production acceptance claim.

The memory extension and root-cause repair were then validated through native
Gerbil only.  One source-first run under a 1 GiB Gambit ceiling passed the
Observability framework, module-system authoring observation, loop-engine
capability, runtime manifest, session registry, and multi-agent graph suites.
The runtime manifest case includes =eq?= assertions proving that presentation,
manifest, and handoff views share the one materialized request graph.  The full
native package build remains red at the pre-existing project policy gate and
is not counted as compile acceptance.

** Remaining integration gates

- [X] Resolve upstream debug source evidence against the pinned =gerbil-poo=
  Git object; compiled-cache and full integration admission remain separate.
- [X] Bind development memory diagnosis to native POO policy/sample/receipt
  values, explicit phase checkpoints, and a distinct launch-time final defense.
- [ ] Reuse =gerbil-poo/debug.ss= for native POO slot and inheritance
  trace projection.
- [ ] Keep =ObservationEvent= immutable and derived from evaluation
  generations.
- [ ] Keep Module at exactly four slots: =identity=, =imports=,
  =capabilities=, and =profiles=.
- [ ] Do not add a fifth observability slot, mutable trace store, or
  public observability DSL.
- [ ] Keep semantic observation separate from debug and recovery
  control.
- [ ] Keep runtime telemetry behind explicit redaction and effect
  boundaries.
- [ ] Prevent runtime clocks, CPU, RSS, and I/O measurements from
  becoming hidden semantic inputs.
- [ ] Apply the native type specialization boundary in
  [[file:45-120-01-poo-native-type-and-contract-prototype-foundation.org][45-120-01]].
- [ ] Apply the Module-local function ownership boundary in
  [[file:45-120-02-module-local-functional-kernel-governance.org][45-120-02]].
- [ ] Apply the observation lineage and recovery-authority boundary in
  [[file:45-120-04-observation-recovery-action-and-telemetry-consumption-boundary.org][45-120-04]].

* References

- [[file:45-120-01-poo-native-type-and-contract-prototype-foundation.org][POO-native Type and Contract prototype foundation]]
- [[file:45-120-02-module-local-functional-kernel-governance.org][Module-local functional kernel governance]]
- [[file:45-122-poo-dispatch-semantic-plane-candidate-resolution-inherited-continuation-and-observability-contract.org][POO dispatch semantic plane and observability contract]]
- [[file:45-124-recursive-module-import-reachability-fixed-point-dispatch-and-cycle-observability-contract.org][Recursive Module import reachability and cycle observability]]
- [[file:45-125-capability-requirement-provision-attenuation-withdrawal-and-reachable-closure-contract.org][Capability reachable closure contract]]
- [[file:45-126-semantic-capability-evidence-runtime-materialization-lease-revocation-and-receipt-contract.org][Semantic capability evidence and runtime materialization]]
- [[file:45-127-higher-order-capability-materialization-poo-lifecycle-object-and-runtime-policy-prototype-algebra.org][Capability materialization lifecycle object]]
- [[file:44-current-project-programming-style.org][Current project programming style]]
- [[file:29-gerbil-poo-programming-guidelines.org][Gerbil POO programming guidelines]]
- =src/module-system/observability.ss=
