#+title: AC-04 Read-Only Shadow Projection Implementation Plan

* Delivery

Implement the approved internal shadow comparator directly in the current
checkout.  Preserve unrelated build and CLI-support changes.  Commit only after
the complete AC-04 qualification pack passes.

* Step 1 — Draft schema cutover

Rename the Bundle and facet schemas to =draft.3=.  Migrate focused assertions
and remove stale pre-release schema names from the AC-01 through AC-04 surface.

* Step 2 — Failing shadow scenarios

Add focused scenarios for equivalent facts, order invariance, the three
difference classes, malformed/duplicate/unstable input, incomplete profiles,
Kernel immutability, draft qualification, and the internal export boundary.

* Step 3 — Internal semantic owner

Add =src/semantic/organization-bundle-shadow.ss=.  Define POO-native fact,
profile, difference, diagnostic, and receipt values.  Project validated Bundle
state into typed facts and compare it with current-path facts using pure
canonical normalization.

* Step 4 — Qualification

Run AC-01, AC-02, AC-03, and AC-04 focused scenarios in the Gerbil environment.
Verify that shadow comparison does not mutate Kernel identity, epoch, status, or
digest and that the new owner is not exported through a public facade.

* Step 5 — Closure

Correct the owner/test matrix and execution status, write the AC-04 receipt,
parse the new Org documents, run =git diff --check=, inspect the path-scoped
diff, and commit the complete feature without unrelated staged changes.
