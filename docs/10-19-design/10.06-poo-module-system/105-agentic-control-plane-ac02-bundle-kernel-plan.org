#+title: AC-02 Immutable Bundle Kernel Implementation Plan

* Delivery

Implement one semantic owner and one focused scenario in the current checkout.
Commit only after AC-02, AC-01, Org, and diff gates pass.  Preserve unrelated
build and CLI-support changes.

* Steps

1. Implement POO-native state, diagnostic, and receipt constructors.
2. Implement pure =open= and =validate= transitions over AC-01 functions.
3. Implement compare-and-advance with phase, identity, epoch, no-op, and next
   Bundle validation gates in that order.
4. Add a focused scenario covering success, replay, immutability, conflicts,
   invalid input, no-op, and deterministic receipts.
5. Run AC-02 and AC-01 scenarios, parse RFC 104–106, update the owner matrix,
   run =git diff --check=, and create one scoped commit.
