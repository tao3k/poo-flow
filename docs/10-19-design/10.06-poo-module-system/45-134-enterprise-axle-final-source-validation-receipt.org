#+title: Enterprise AXLE Final Source Validation Receipt
#+startup: overview

* Status

Executed and incomplete.

This receipt records the 2026-07-28 dual validation of the existing enterprise
Lean proof family.  Local Lean is green.  AXLE independent final-source
validation is =7/14= for leaf modules and therefore remains open.

The architecture decision for closing this gap is proposed in
[[file:45-135-axle-independent-declaration-bundle-and-environment-refinement.org][45-135]].
Its first isolated manifest implementation is recorded in
[[file:45-136-independent-declaration-bundle-manifest-implementation-receipt.org][45-136]].

The receipt supports
[[file:45-131-external-evidence-obligation-and-certificate.org][45-131]],
[[file:45-132-2026-h1-enterprise-identity-authorization-and-audit-evidence.org][45-132]],
and
[[file:45-133-2026-h1-agent-tool-memory-and-supply-chain-evidence.org][45-133]].
It does not discharge their new proof obligations.

* Tool identity

| Surface | Identity |
|---------+----------|
| Python environment | uv project at =packages/proof/python= |
| AXLE distribution | =axiom-axle= |
| AXLE version | =1.4.0= |
| AXLE environment | =lean-4.31.0= |
| AXLE mode | =independent= |
| Executor commit | =160ed94= |
| Executor image | =sha256:7e8f59ca21bd1acd4d5dbbc6748bdf5be02633e2a671d9dcf064584a6a1cf1e0= |
| Executor artifact | =61ee864a5f318a708d331c6649a483939963279051b4d1e20e13718331914854= |

* Commands and results

#+begin_src text
cd packages/proof/python
uv run pytest -q tests/test_axle_verify.py
# 1 passed in 0.02s

cd packages/proof/lean
lake build PooFlowProof.PooC3.Enterprise
# Build completed successfully: 177 jobs

cd packages/proof/python
uv run python -m poo_flow_proof.axle_verify \
  ../lean/PooFlowProof/PooC3/Enterprise/<leaf>.lean
# 14 leaf inputs: 7 verified, 7 rejected before verify
#+end_src

The aggregate =Enterprise.lean= is not a valid AXLE theorem input: it contains
imports but no theorem or definition, and was rejected with
=No theorem or definition found in formal_statement=.  Aggregates remain stable
import surfaces; AXLE validation targets declarations in leaf modules.

A focused preserved-imports diagnostic was also executed:

#+begin_src text
uv run python -m poo_flow_proof.axle_verify --preserve-imports \
  ../lean/PooFlowProof/PooC3/Enterprise/Core.lean
# request: a3652e6b-eaea-47eb-9e31-657da9d0a404
# rejected: unknown module prefix 'PooFlowProof'
#+end_src

The AXLE environment contains Lean, Mathlib, and its prover libraries, but not the
local =PooFlowProof= package.  Therefore =--preserve-imports= is not a closure
mechanism.  The project must either construct an exact declaration bundle or
publish a versioned AXLE environment containing the matching project artifacts.

* Dual-engine result

#+begin_src typst
$ "LocalLeanGreen"("Enterprise") $

$ "AxleVerifiedLeaves" = 7 $

$ "EnterpriseLeaves" = 14 $

$ "AxleFamilyClosed" <=>
  "AxleVerifiedLeaves" = "EnterpriseLeaves" $

$ 7 < 14 => not "AxleFamilyClosed" $

$ "LocalLeanGreen" and not "IndependentDependencyClosure"
  => not "AxleFamilyClosed" $
#+end_src

The final formula is the central receipt boundary: a successful project build
does not imply independent final-source closure.

* Leaf validation matrix

| Leaf module | AXLE | Request identity or first missing closure |
|-------------+------+-------------------------------------------|
| =Enterprise/Assurance.lean= | pass | verify request =96328ddc-91d5-42fd-b003-92b035495bf5= |
| =Enterprise/EvidenceFreshness.lean= | pass | verify request =62541b23-0373-4a19-a8f5-399ae179e7b8= |
| =Enterprise/ResponsibilityTransfer.lean= | pass | verify request =25fd13f1-473d-40c2-8810-4430d7efb9cb= |
| =Enterprise/TenantOrganization/CacheIsolation.lean= | pass | verify request =073bb099-620c-485e-8bd9-2b69011c151c= |
| =Enterprise/TenantOrganization/Core.lean= | pass | verify request =5d4460a4-9ed2-41af-a2aa-2202ca931709= |
| =Enterprise/TenantOrganization/EvidenceVisibility.lean= | pass | verify request =fd17f0ac-b564-4966-ac0f-22ae7ba09c45= |
| =Enterprise/TenantOrganization/Identity.lean= | pass | verify request =4ef8cbb4-30aa-402d-b80c-6ee4c34dd72e= |
| =Enterprise/Core.lean= | fail | missing contributor qualification and governance declarations |
| =Enterprise/Delegation.lean= | fail | missing =CrossDomainFederation.CapabilityAttenuation= |
| =Enterprise/Admission.lean= | fail | missing Core, governance, duty-separation, and freshness declarations |
| =Enterprise/SeparationOfDuties.lean= | fail | missing governance quorum and separation declarations |
| =Enterprise/TenantOrganization/Admission.lean= | fail | missing enterprise admission and scope witnesses |
| =Enterprise/TenantOrganization/CedarAdapterRefinement.lean= | fail | missing Cedar adapter, arbitration, and entity-closure declarations |
| =Enterprise/TenantOrganization/CedarEntityClosure.lean= | fail | missing scope, identity, Cedar Spec, and arbitration declarations |

No failed leaf reached the AXLE =verify= phase.  Each was rejected during
=theorem2sorry= because its independent formal statement lacked declarations
that the local project import graph supplies.

* Closure topology

#+begin_src mermaid
flowchart LR
    L["Local lake build"] -->|"177 jobs; project imports available"| G["Local Lean green"]
    U["uv run axle_verify"] -->|"replace project imports; independent source"| M["14 leaf matrix"]
    M -->|"7 declaration-complete"| P["AXLE verified"]
    M -->|"7 missing dependency closure"| O["Open final-source obligations"]
    G -->|"does not imply independent closure"| O
    O -->|"bundle exact declarations without weakening theorem"| R["Re-run AXLE"]
    R -->|"14 of 14 plus stable receipt"| C["Eligible for explicit certificate"]
#+end_src

* Interpretation

- The uv dependency and project wrapper are functioning.
- The Python harness test is green.
- The local Lean proof family is internally buildable through its project imports.
- Half of the leaf modules are already independently verifiable.
- The seven failures identify declaration-closure boundaries that must be made
  explicit; they are not evidence that the stated theorems are false.
- Preserving imports does not solve the boundary because the remote AXLE
  environment cannot resolve the local =PooFlowProof= module prefix.
- Cedar failures remain within the POO Flow =CedarAdapter= proof boundary.  They
  do not create a new Rust owner or make Cedar runtime internals POO Flow owners.
- New external-evidence, tool-authority, memory-lifecycle, workflow-sequence, and
  supply-chain modules proposed by 45-131 through 45-133 are still absent and
  cannot inherit the seven passing receipts.

* Closure checklist

- [X] Resolve AXLE through the uv-managed Python dependency.
- [X] Verify the project wrapper CLI.
- [X] Run the Python AXLE harness test: =1 passed=.
- [X] Run the local enterprise Lean target: =177 jobs=.
- [X] Run all 14 declaration-bearing enterprise leaf modules.
- [X] Preserve local Lean and AXLE as separate evidence fields.
- [ ] Construct an exact independent declaration closure for =Enterprise/Core.lean=.
- [ ] Construct the cross-domain capability closure for =Enterprise/Delegation.lean=.
- [ ] Close enterprise admission and separation-of-duties dependencies.
- [ ] Close tenant admission dependencies without duplicating ownership.
- [ ] Project Cedar Spec and POO Flow Cedar adapter dependencies into exact final source.
- [ ] Review and accept the bundle-primary, environment-refinement decision in 45-135.
- [ ] Re-run all 14 leaves and require =14/14=.
- [ ] Implement and verify the new proof owners proposed by 45-131 through 45-133.
- [ ] Issue an explicit certificate only after every required surface is green.
