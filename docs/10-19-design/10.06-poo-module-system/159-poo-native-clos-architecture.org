# SPDX-FileCopyrightText: 2026 tao3k team and Contributors
#
# SPDX-License-Identifier: Apache-2.0 AND LGPL-2.1-or-later

#+title: POO-Native CLOS Architecture
#+startup: overview

* Scope and acceptance

This document defines the stable architecture and acceptance boundary for the
POO-native CLOS extension.  It is not a delivery plan.  Implementation order,
temporary slices, and historical test-run identifiers are deliberately absent.

The Common Lisp HyperSpec is a semantic reference and conformance-vector source
for class, instance, generic-function, method, method-combination,
class-change, class-redefinition, and load-form behavior.  It does not own the
runtime architecture.  POO CLOS extends Gerbil POO's metaobject protocol and
may explicitly reject a reference behavior that would duplicate or contradict
the native object model.

Acceptance is executable and fail-closed:

- every supported reference operator has a source-owned evidence fixture;
- every semantic clause names an owner plus positive and negative tests;
- every closed evidence identity resolves to an exact exported =std/test= suite
  whose declared case cardinality matches and whose cases contain checks;
- no semantic acceptance is reported while a required POO CLOS row is open.

Package compilation and artifact ownership remain exclusively in the native
=build.ss=/=std/make= graph.  The semantic ledger neither recompiles fixtures nor
makes an artifact-provenance claim.

* Ownership boundary

The POO-native CLOS owner is =src/module-system/poo-clos/=.  Public values are
native Gerbil POO objects, and declarations lower hygienically to those objects
plus functional dispatch operations.  No second method-combination module is
part of the active Loader, profile, Funflow, or user-interface graph.

This owner is a POO-native CLOS metaprotocol extension, not an ANSI
compatibility layer.  Its lightweight descriptor participates in the kernel
selection, while the runtime facade is lazy and opt-in and is not imported or
re-exported by =src/module-system/api.ss=.  Importing the general Module System
facade must not load CLOS dispatch, lifecycle generics, declaration IR, or
method-combination code.

Native POO owns object identity, Type/Class/Slot metaobjects, prototype
inheritance, slots, and C3.  The CLOS extension owns only the semantics that
coordinate multiple arguments, specializers, effective methods, class
generations, and invocation frames.  Class precedence is projected from the
instance prototype's native C3; POO CLOS must not compute a parallel CPL.  It
does not wrap a Common Lisp runtime, expose raw alist DSLs, or construct a
parallel record-based object hierarchy.

#+begin_export mermaid
flowchart LR
    DECL["Hygienic CLOS declaration"] -->|"parse and validate"| IR["Bounded declaration IR"]
    IR -->|"lower"| META["Native POO metaobjects"]
    META -->|"applicability and precedence"| EFFECTIVE["POO effective method"]
    EFFECTIVE -->|"invoke"| FRAME["Lexical invocation frame"]
    FRAME --> RESULT["Scheme values"]
    FRAME -->|"typed rejection"| FAILURE["POO CLOS condition"]
    META -->|"reinitialize in place"| GENERATION["Stable identity, new generation"]
    GENERATION -.->|"history and qualification only"| RECEIPT["Qualification evidence"]
#+end_export

* Stable contract areas

| Contract area | Required semantics |
|---------------+--------------------|
| Metaobjects | POO class, slot, instance state, slot cell, specializer, method, generic function, effective method, invocation frame, and condition values |
| Specialization | class, =eql=, universal, multiple dispatch, and argument precedence order |
| Class precedence | native Gerbil POO C3, projected back to the exact POO CLOS class metaobjects |
| Method management | add, remove, lookup, same-signature replacement, generation tracking, and cache invalidation |
| Standard combination | around, before, primary, after, multiple values, and exact next-method applicability |
| Custom combination | all ANSI built-ins plus declarative short and long forms, method groups, ordering, required groups, arguments, generic-function binding, =call-method=, and =make-method= |
| Lambda lists | required, optional, rest, key, allow-other-keys, supplied-p, auxiliary bindings, and congruence |
| Classes and slots | direct superclasses, direct/effective slots, initforms, initargs, readers/writers, and instance/class allocation |
| Instance lifecycle | allocate, initialize, shared-initialize, reinitialize, slot-boundp, slot-makunbound, slot-missing, and slot-unbound |
| Evolution | change-class, update-instance-for-different-class, class redefinition, dependency propagation, explicit obsolescence, and lazy instance convergence |
| Load forms | two-phase creation/initialization, cycle-safe reference resolution, selected-slot saving, unbound preservation, and class-name association |
| Declaration surface | POO-native =.defclass=, =.defgeneric=, =.defmethod=, and =.define-method-combination= |
| Reflection | defensive class, slot, method, generic, specializer, dependency, and generation projections |
| MOP-EXTENDED | sealed read-only portable profile outside the POO CLOS core acceptance denominator |

The executable clause and operator manifests live in
=t/qualification/poo-clos/clause-ledger.ss=.  Ledger rows contain semantic
identity, profile, owner, and positive/negative evidence.  They intentionally
contain no project-management stage or delivery-sequence field.

* Declaration and runtime interfaces

The declaration parser produces immutable, phase-owned declaration IR values.
Runtime construction, method selection, effective-method computation, and
invocation remain ordinary POO-native or functional operations.  Raw
=(lambda (self super) ...)= hooks and private patch languages are not ordinary
authoring interfaces.

The explicit-frame kernel form remains available to low-level implementations:

#+begin_src scheme
(def generic
  (poo-clos-generic-function
   'route 2 argument-precedence-order: '(1 0)))

(poo-clos-add-method
 generic
 (poo-clos-method
  'route-by-class
  (list (poo-clos-class-specializer request-prototype)
        (poo-clos-eql-specializer 'priority))
  (lambda (frame request mode)
    (if (poo-clos-next-method? frame)
      (poo-clos-call-next-method frame request mode)
      request))))
#+end_src

Ordinary authoring uses the hygienic declaration forms, which supply lexical
next-method operations without exposing the frame argument.

* Deliberate Scheme and POO adaptations

- Class and generic-function redefinition reinitialize an existing POO object
  in place.  Identity is stable; generation and layout history carry only the
  information required for cache invalidation and lazy migration.
- CLOS =eql= specializers use Scheme =eqv?= without serialization or numeric
  canonicalization.
- Initargs and generic keywords accept native Gerbil keywords and symbols;
  =allow-other-keys:= keeps the ANSI admission role.
- Multiple values use Scheme's native values protocol.
- =make-load-form-saving-slots= returns a creation thunk and an initialization
  procedure.  The initialization procedure accepts an optional reference
  resolver so cyclic graphs can be restored without embedding a serialization
  DSL.  Initforms are never replayed by this protocol.

* Qualification fixtures

Qualification groups tests only where a stable semantic fixture needs its own
source-owned executable evidence boundary:

- method-combination compatibility;
- generic dispatch;
- class and instance lifecycle;
- declaration surface;
- method combination;
- evolution, reflection, and load form;
- POO CLOS reference-clause ledger.

Fixture names describe durable evidence boundaries, not delivery stages.  The
ledger resolves them to exact exported native test suites and verifies case and
check presence with the Scheme reader.  ASP Testing owns optional memory and
machine observation profiles at the test-process boundary.  The ledger makes
neither an artifact-provenance nor a performance claim; performance requires a
separately approved workload and SLA.

* Sources

- [[https://www.lispworks.com/documentation/HyperSpec/Body/04_c.htm][CLHS 4.3: Classes]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/07_e.htm][CLHS 7.5: Slots]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/07_f.htm][CLHS 7.6: Generic Functions and Methods]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/07_ffb.htm][CLHS 7.6.6.2: Standard Method Combination]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/07_ffd.htm][CLHS 7.6.6.4: Built-in Method Combination Types]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/m_defi_4.htm][CLHS: define-method-combination]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/f_mk_ld_.htm][CLHS: make-load-form]]
- [[https://www.lispworks.com/documentation/HyperSpec/Body/f_mk_l_1.htm][CLHS: make-load-form-saving-slots]]
- =.data/gerbil-poo/object.ss= for native POO objects, slots, and C3.
- =.data/gerbil-poo/mop.ss= for the upstream generic-function boundary.
