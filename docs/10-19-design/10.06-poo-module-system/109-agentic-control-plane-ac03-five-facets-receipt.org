#+title: AC-03 Five Typed Facets Qualification Receipt

* Result

The Canonical Organization Bundle is cut over to schema v2 with five mandatory
POO-native facets: Organization, Authority, Context, Protocol, and Evidence.
Typed-empty constructors represent empty facets; the flat Bundle constructor is
removed.

* Semantic closure

- Canonical normalization emits facets in fixed RFC order.
- Facets share one Bundle identity, epoch, and validation result.
- Shared ids with incompatible entity kinds fail closed.
- Protocol participants and capabilities resolve across Organization and
  Authority facets.
- Evidence subjects and targets resolve across Organization and Protocol facets.
- Missing or non-POO facets fail closed.
- AC-02 Kernel consumes Bundle v2 without changing its public API.

* Qualification

| Gate | Result |
|------+--------|
| AC-03 focused scenario | PASS — 5 cases / 6 checks |
| AC-01 canonical Bundle | PASS — 8 cases / 20 checks |
| AC-02 immutable Kernel | PASS — 6 cases / 19 checks |
| Semantic module compilation | PASS |
| Org provider parsing | PASS |
| =git diff --check= | PASS |

AC-03 does not implement C ABI layout, Python runtime behavior, Cedar policy,
Lean proof generation, persistence, or runtime effect execution.
