#+title: Native Public Closure and Warm-Build Convergence
#+language: en

* Status

This module records the executable diagnosis of the native Gerbil warm-build
regression.  It is a design and acceptance record, not permission to add a
POO-Flow-local scheduler, cache, dependency parser, or shell launcher.

The verified conclusion is narrower than the original scheduler hypothesis:

- Gerbil =std/make= parallel execution is enabled.
- A reverse-ordered dependency scenario converges correctly.
- The complete 387-entry root build can converge to zero native compilation.
- Its zero-work scan still takes 63.46 seconds and therefore fails the
  30-second warm-build gate.
- The three public entry interfaces currently have a 45-module all-phase
  closure.  The integrated zero-work build now completes near five seconds.
- ASP Build API owns root-only Import Model traversal; POO Flow declares only
  its public entries.  Gerbil =std/make= remains the planner and executor.

* Verified execution receipts

| Scenario | Result | Native work | Meaning |
|----------+--------+-------------+---------|
| Four-module owner-map warm replay | 5.30 s | zero compile jobs | A small existing DAG is stable. |
| Reverse-ordered two-module dependency update | 9.78 s | dependency then consumer | =std/make= restores topological order. |
| Immediate two-module warm replay | 9.11 s | zero compile jobs | Deferred native jobs did not create a second invalidation wave. |
| Full root recovery build | 474.18 s | parallel native jobs | The previously interrupted root was not a qualified warm state. |
| Full root qualified warm replay | 63.46 s | zero compile jobs | Correct incrementality, failed latency gate. |
| Public closure warm replay 1 | 22.65 s | zero compile jobs | Passed latency and work gates. |
| Public closure warm replay 2 | 19.40 s | zero compile jobs | Passed latency and work gates. |
| Public closure warm replay 3 | 20.70 s | zero compile jobs | Passed latency and work gates. |
| Full 489-target warm replay before root projection | 96.90 s | zero compile jobs | Reproduced excessive native planning input. |
| Direct 45-target =std/make= control | 2.68 s | zero compile jobs | Upstream planner is fast for the correct input shape. |
| Integrated 45-target warm build | 3.76 s | zero compile jobs | Passed the eight-second acceptance ceiling and five-second target. |
| Isolated 45-target clean build | 40.57 s | 45 compile jobs | Proved the closure is complete without prior POO Flow outputs. |
| ASP three-target process Scenario | 1.50 s | projection only | Exact =a,b,c= closure excludes the unrelated module. |

The project-path metadata audit observed 387 loadable modules and 1,861 direct
POO dependency edges.  Before the recovery build it reported 121 timestamp
inversions.  After the recovery build and correct output classification it
reported:

#+begin_example
spec_entries=387
modules_checked=387
direct_poo_edges=1861
inversions=0
missing_outputs=0
skipped=0
#+end_example

An SSI file is the authoritative completion output for a syntax-only module.
An old zero-byte static SCM file is not allowed to override a newer SSI.  This
rule removed 28 false inversions attributed to =src/user-interface/init-syntax=.

* Architectural boundary

#+begin_src mermaid
flowchart LR
  Entry[Public POO entry interfaces] --> Closure[Package-aware all-phase closure]
  Closure --> Spec[Minimal native-spec]
  Spec --> Make[Gerbil std/make]
  Make --> SSI[SSI and non-empty static outputs]
  Make --> Receipt[Scheme JSON receipt]

  Tools[Tools] --> FeatureTargets[Explicit feature targets]
  UI[User interfaces] --> FeatureTargets
  Fixtures[Tests and fixtures] --> FeatureTargets
  Bindings[Bindings and generators] --> FeatureTargets
  FeatureTargets --> Make

  ASP[ASP Build API] --> Closure
  Gerbil[Gerbil module metadata] --> Closure
  POO[POO Flow build.ss] --> Spec

  BadLocal[POO-local parser, scheduler, or cache] -. forbidden .-> Spec
#+end_src

The default library build must not own tools, fixtures, language bindings,
benchmarks, generated-vector commands, and user-interface scenarios.  Those
surfaces remain buildable through explicit feature or target declarations.
They are not deleted and they are not silently omitted from release assurance.

The current 387-entry profile contains 379 =src/= entries and eight
=user-interface/= entries.  The dominant source owners are
=src/modules/= (132), =src/module-system/= (129), =src/core/= (27),
=src/contract/= (21), and =src/feature-system/= (15).  This distribution makes
the next split concrete: capability modules and broad module-system surfaces
must move behind explicit feature ownership.  Directory membership alone is
not a closure rule; each feature target must be validated from its public entry
interfaces and all syntax/runtime dependency phases.

* ASP Build API resolution

The PackageSpec prototype now exposes =public-entry-modules=.  Its projector
derives package identity from the downstream =gerbil.pkg=, imports only the
declared roots, and traverses Gerbil's native =module-context-import= graph once
with an identity-keyed visited table.  External package contexts remain owned
by =gxpkg= and are not traversed into the downstream build target set.

This is intentionally different from the removed historical implementation,
which called =import-module= for every catalog member before calculating extra
inputs.  The root-only algorithm is O(V+E) in the reachable local graph and
does not parse source, schedule compilation, or inspect =GERBIL_PATH= outputs.

The stable downstream declaration is:

#+begin_src scheme
(asp-gerbil-scheme-package-spec!
 (poo-flow-library-package-spec
  @ asp-gerbil-scheme-library-package-prototype)
 (spec poo-flow-library-spec)
 (public-entry-modules
  '("src/core/api.ss"
    "src/module-system/interface.ss"
    "src/feature-system/interface.ss")))
#+end_src

The ASP process Scenario additionally starts without package-local outputs in
the acceptance environment.  The projector mirrors =std/make='s native
=add-load-path! srcdir= acquisition before importing roots, which prevents warm
artifacts from hiding a clean-build resolution failure.

* Promotion gates

A change is promotable only when all of the following hold:

1. The ASP closure API accepts arbitrary Gerbil package identity and has a
   non-ASP downstream scenario test.
2. A clean sandbox build proves that the public closure contains every runtime
   and syntax-phase dependency.
3. POO Flow =build.ss= declares only package role, profile, public entries, and
   explicit feature targets.
4. The ASP downstream closure Scenario takes at most five seconds; the bounded
   POO public-library warm receipt takes at most eight seconds and starts no
   Gerbil or GCC compilation job.  Comparative gates use the existing same-host
   relative receipt contract instead of manual replay loops.
5. The full release target still builds and tests all explicitly owned tools,
   bindings, fixtures, and user-interface scenarios.
6. Scheme observability emits JSON receipts with package identity, profile,
   closure digest, module count, elapsed milliseconds, cache state, effective
   host-derived core count, and peak RSS.
7. The tracked Bazel module lock contains both Linux and Darwin entries for the
   pinned ASP revision.

The public-library build gates now pass.  Full release target coverage, JSON
receipt emission, and the cross-platform Bazel lock remain separate promotion
requirements; a fast public build does not make those gates implicitly green.
