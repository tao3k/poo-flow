#+title: AC-09 Lean / Evidence Proof Bridge Qualification Receipt
#+date: 2026-07-12
#+status: qualified

* Decision

AC-09 is qualified as the Lean/evidence proof bridge and as a completed Bundle
v1 component.  The AuthorizedEffectToken proof chain is implemented across the
Scheme POO control plane, runtime-v0 native kernel, public C ABI, production
Python CFFI runtime, and Lean theorem package.

This qualification does not freeze the public ABI and does not recreate the
retired graph ABI.

=abi-v1-frozen? #f=

Qualification base commit: =50339d2=.

* Evidence flow

#+begin_src mermaid
flowchart TD
    MANIFEST["Canonical ProofCaseVector v1 manifest"] --> SCHEME["Scheme POO native encoder"]
    MANIFEST --> LEAN["Lean typed decoder and theorem set"]
    SCHEME --> CABI["Runtime-v0 caller-owned bulk C ABI"]
    CABI --> PYTHON["Installed Python CFFI runtime"]
    SCHEME --> DIFF["Four-way byte, field, and semantic differential"]
    CABI --> DIFF
    PYTHON --> DIFF
    LEAN --> DIFF
    LEAN --> PROOF["AuthorizedEffectToken ProofReceipt"]
    DIFF --> QUAL["AC-09 qualification receipt"]
    PROOF --> QUAL
#+end_src

* Canonical identity receipt

- Schema fingerprint:
  =bad9c5d0781d0a99e2f8d58cb94abae9dfc2eda4c71a01009897f7fc5419e0e7=.
- Canonical 424-byte vector digest:
  =970eaffbfaae38970e2107b89fa11258c6211a5c293a1e400a464527b7a0e44a=.
- Vector digest domain: =poo-flow.proof-case-vector.v1=.
- Theorem-set digest domain:
  =poo-flow.authorized-effect-theorem-set.v1=.
- The manifest generates the C, Python, Lean, and fixture projections; the
  generated-artifact freshness check passes.

* Scheme and control-plane receipt

- The public proof-case route is POO-native and retains the functional
  control-plane boundary; no raw alist or record DSL became a public API.
- Scheme emits the exact caller-owned 424-byte native vector and the
  manifest-owned domain-separated digest.
- Proof-case projection: 21 checks passed.
- Proof-case vector: 14 checks passed.
- Authorized-effect-token: 26 checks passed.
- Batched Merkle evidence: 13 checks passed.
- Control-plane handoff, proof-gate Bundle, decision, receipt, and FFI owners
  passed.

* Native C ABI receipt

- The production surface is =measure/write= over caller-owned storage; JSON and
  Gerbil GC/object layouts do not cross the ABI.
- Normal, installed-consumer, ASan, UBSan, leak, and benchmark-smoke targets
  passed: 14 of 14 Bazel tests with cached results disabled.
- Strict and Batched use three C calls per item, copy 424 bytes per item, and
  perform zero timed per-item allocations on the whole-vector path.
- Root-history lookup is owned by =runtime_v0.c::pending_lower_bound=: binary
  search followed by the matching range, hence =O(log n + k)=.

* Lean proof receipt

- =lake build= completed 56 jobs and =lake exe ffiSmoke= passed.
- The typed decoder covers the full fixed-width vector.
- =verifiedProofReceipt= derives the vector and theorem-set digests internally;
  callers cannot inject either identity.
- The theorem set verifies policy/effect binding, semantic and execution roots,
  obligation completeness, replay protection, Diagnostic non-execution, and L3
  completeness.
- L3 requires the complete verified mediation and root-linked inclusion chain;
  signature presence or a buffered item alone cannot claim L3.

* Python and differential receipt

- Production Python runtime suite: 190 passed, 8 skipped.
- Proof Python suite: 40 passed; Ruff and generated-artifact freshness passed.
- The isolated, non-editable installed-wheel consumer passed without checkout
  =PYTHONPATH= or Gerbil at runtime.
- Scheme, canonical fixture, C round trip, Python CFFI, and Lean fixture agree
  at byte, field, semantic, and digest levels.
- Layout and fingerprint negotiation occurs once per runtime instance.  The
  steady-state CFFI path reuses thread-local scratch storage and takes three
  crossings per item.

* Security and determinism receipt

- Table-driven structural mutations cover unknown tags, unsupported masks,
  reserved boundaries, malformed sizing, invalid handles, and mutable borrowed
  buffer TOCTOU; they fail closed at the native/Python boundary.
- Token, policy, effect, subject, resource, action, semantic root, execution
  root, batch root, nonce, epoch, session, lease, and batch substitutions change
  proof identity and report the first divergent owner, field, and byte offset.
- Strict mediation, Batched roots and inclusion receipts, and Scheme vector
  emission are deterministic across fresh processes.
- Omitted receipts, missing obligations, replay/fork attempts, unsupported
  obligations, and malformed vectors cannot produce executable authorization.

* Performance receipt

The first supported-host measurements are baselines, not ABI or latency
thresholds:

- Native receipt:
  =bindings/runtime-c/benchmarks/receipts/proof_case_v1_macos_arm64.receipt=.
- Installed-wheel CFFI receipt:
  =packages/proof/python/benchmarks/receipts/proof_case_cffi_wheel_macos_arm64.receipt=.
- Native batches 1, 8, 32, 128, and 1024 exercise the real Strict/Batched
  caller-owned whole-vector path.
- Python steady state performs three crossings per item after one layout
  measurement per runtime instance.
- No concrete p50/p99 budget is frozen from a single host series.

* Regression receipt

- AC-01 canonical organization Bundle: 20 checks passed.
- AC-02 organization Bundle kernel: 19 checks passed.
- AC-03 five facets: 6 checks passed.
- AC-04 shadow execution: 18 checks passed.
- AC-05 runtime-v0: 6 checks passed.
- AC-06 runtime-v0 batch: 8 checks passed.
- AC-07 production Python runtime: 190 passed, 8 skipped.
- AC-08 authorization, evidence, proof-gate, and FFI owner suites passed.
- AC-09 Scheme, C, Python, Lean, installed-consumer, security, differential,
  determinism, performance, sanitizer, and leak gates passed.

* Retired-surface audit

A bounded search across =bindings=, =src=, =packages=, and the AC-09 owner RFC
found no graph ABI, =graph_abi=, =graph-abi=, compatibility wrapper, or
=draft_1= candidate.  No compatibility surface is retained.

* Toolchain receipt

- Scheme gates used the repaired local Homebrew Gerbil 0.18.2 at
  =/opt/homebrew/bin/gxi=, never the Nix Gerbil.
- The explicit CommandLineTools SDK and repository =GERBIL_LOADPATH= isolated
  the qualification from the host's Nix-selected SDK and stale package-tag
  views.  No package uninstall or destructive retagging was performed.

* Next boundary

AC-10 integrated qualification is next.  It may consume this qualified Bundle
v1 proof component, but only a later supported-host and compatibility decision
may change =abi-v1-frozen?=.
