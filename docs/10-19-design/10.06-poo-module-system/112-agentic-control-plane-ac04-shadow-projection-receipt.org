#+title: AC-04 Read-Only Shadow Projection Qualification Receipt

* Status

Complete on 2026-07-11.  AC-04 is qualified as draft migration machinery.  It
does not publish or imply Canonical Organization Bundle v1.

* Delivered

- Bundle schema =poo-flow.organization-bundle.draft.3=.
- Facet schema =poo-flow.organization-facet.draft.3=.
- Internal semantic owner
  =src/semantic/organization-bundle-shadow.ss=.
- POO-native shadow facts, explicit comparison profiles, typed diagnostics, and
  deterministic receipts.
- Separate =missing-current=, =missing-bundle=, and =mismatched-values= sets.
- Fail-closed duplicate fact, unknown facet, unstable value, and incomplete
  profile handling.
- Every shadow receipt reports =v1-conformant? #f=.

* Boundary evidence

ASP owner discovery finds =organization-bundle-shadow= only in the semantic
owner package under =src=.  No canonical public facade imports or re-exports the
shadow module.  Comparison consumes a validated/advanced immutable Kernel state
and does not call Kernel advance, a runtime, the future C ABI, or an effect path.

* Qualification results

| Gate | Result |
|------+--------|
| AC-01 canonical Bundle scenarios | 8 cases, 20 checks, pass |
| AC-02 immutable Kernel scenarios | 6 cases, 19 checks, pass |
| AC-03 five-facet scenarios | 5 cases, 6 checks, pass |
| AC-04 shadow scenarios | 6 cases, 18 checks, pass |
| Gerbil semantic owner compilation | pass |
| Public facade search | semantic owner only |
| Bundle v1 claim | absent; receipt is explicitly false |

All Gerbil commands ran with Nix compiler include/library variables removed and
the checkout parent supplied as =GERBIL_LOADPATH=.  The three semantic owners
were compiled in dependency order before the focused scenario run so stale
library artifacts could not mask the schema cutover.

* Architecture receipt

#+begin_src mermaid
flowchart LR
  Current["Current typed semantic facts"] --> Compare["Pure internal comparator"]
  Kernel["Validated Bundle Kernel draft.3"] --> Projection["Bundle typed fact projection"]
  Projection --> Compare
  Profile["Explicit facet and path profile"] --> Compare
  Compare --> Receipt["Deterministic shadow receipt"]
  Receipt --> Draft["Migration evidence; v1-conformant is false"]
#+end_src

* Deferred

AC-05 owns the versioned C ABI control lane.  AC-06 owns hot/bulk performance
lanes, including the later =.data/gerbil= and =.data/gerbil-mysql= evidence
review.  AC-07 owns the production-capable Python CFFI validation runtime.
Cedar and Lean/Evidence remain in their later acceptance packages.  Bundle v1
remains reserved for the complete stable-chain freeze.
