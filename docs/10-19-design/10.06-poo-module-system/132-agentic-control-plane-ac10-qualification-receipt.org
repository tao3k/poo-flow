#+title: AC-10 Integrated Qualification Receipt
#+date: 2026-07-12
#+status: qualified

* Decision

AC-10 is qualified.  Clean revision =0206ff3= traversed every accepted Scheme,
native C, installed Python, evidence, and Lean boundary and produced a validated
=ReleaseAssuranceManifest v1=.  AC-11 remains blocked on separate migration
approval.

=abi-v1-frozen? #f=

* Qualified evidence flow

#+begin_src mermaid
flowchart LR
    POO["POO authoring and five-facet Bundle"] --> CABI["Installed runtime-v0 C ABI"]
    CABI --> PY["Production Python CFFI and isolated wheel"]
    PY --> AUTH["Cedar token and mediated effect"]
    AUTH --> ROOTS["Execution and evidence roots"]
    ROOTS --> PROOF["ProofCaseVector and Lean named claims"]
    PROOF --> CLAIMS["Five claim-specific TCB receipts"]
    CLAIMS --> MANIFEST["ReleaseAssuranceManifest v1"]
#+end_src

* Manifest identity

- Source revision: =0206ff3=.
- Release id: =ac10-release-candidate=.
- Manifest digest:
  =968d37adf2ea1008f535020379985b0d00b75a1f465610bdd2633f6660b06035=.
- Canonical Bundle digest:
  =db1b99c9640ce9d5664ad70efb79280a918093a2053da2b13d3b3717fc87ed5b=.
- ProofCaseVector digest:
  =dad236461f018a3c8f7226b9b9335785a73288120c0fe4dc7eb73ac0a2133f09=.
- Proof schema fingerprint:
  =bad9c5d0781d0a99e2f8d58cb94abae9dfc2eda4c71a01009897f7fc5419e0e7=.
- Claim count: 5; gate count: 12.

* Claim and TCB receipt

| Claim | Level | TCB | Required evidence |
|-------+-------+-----+-------------------|
| Canonical Bundle | L1 Conformant | Bundle semantic | Scheme canonical fixture |
| Authorized effect | L2 Evidenced | Authorization | Python proof/security owner |
| Mediated effect | L3 Mediated | Actual effect | Native runtime and sanitizer observation |
| Evidence history | L2 Evidenced | Evidence history | Leak and performance owners |
| AuthorizedEffectToken theorem set | L4 Formally Linked | Lean claim | Locked Lean build and native FFI smoke |

All five claim receipts passed.  L3 requires runtime-kernel evidence and
isolation or independent observation.  L4 applies only to the named theorem set
and cannot promote an actual-effect claim.

* Release gates

- Canonical Scheme fixture: 13 checks passed.
- Runtime-v0 and proof-case installed C consumers: passed.
- Production Python runtime: 190 passed, 8 skipped.
- Isolated proof wheel with =PYTHONPATH= removed: 1 passed.
- Lean build: 56 jobs; native FFI smoke: passed.
- Native functional owners: 7 of 7 passed.
- ASan, UBSan, and contract parity owners: 7 of 7 passed.
- Native leak owner: 1 of 1 passed.
- Python proof/security/differential suite: 40 passed.
- Performance-matrix verifier: 7 checks passed.

* Fault and security receipt

The qualified owners cover deny, replay, revocation, binding substitution,
stale proof, timeout, cancellation, runtime failure, indeterminate mediation,
fork, truncation, mutable-buffer TOCTOU, malformed vectors, sanitizer faults,
leaks, and installed-consumer isolation.

A bounded retired-surface audit found no =graph_abi=, =graph-abi=, =draft_1=,
compatibility wrapper, or private-runtime-bypass candidate across =bindings=,
=src=, and =packages=.  No graph ABI compatibility layer is retained.

* Performance receipt

The native receipt has the complete supported Cartesian matrix:

- batches: 1, 8, 32, 128, 1024;
- payload bytes: 0, 1024, 65536, 1048576;
- dispositions: allow, deny, success, runtime-failure, timeout, cancel,
  indeterminate;
- total blocks: 140;
- lookup: =O(log n + k)=;
- vector construction: =O(1)= per item and =O(k)= per batch;
- proof profiles: Strict and Batched, native and installed CFFI;
- steady proof ABI crossings: 3 per item;
- caller-output allocations per item: 0.

Restore performance has no runtime benchmark owner and is explicitly
unsupported.  Absolute latency budgets remain unsupported until a reproducible
multi-host series exists.  Current numbers are =baseline-only=.

* Scheme guard receipt

The Building Framework =define-guarded-gxi-stage= extension owns the Scheme RSS
and timeout boundary.  It terminates allocating or stalled child =gxi=
processes fail-closed and returns a POO-native receipt.  The canonical fixture
measured approximately 274 MiB peak RSS under a 512 MiB ceiling after large POO
constructor optimization.

* Cutover boundary

Bundle v1 components and the integrated path are qualified.  This does not
freeze ABI v1, approve latency budgets, or authorize AC-11 removal.  A separate
migration decision must approve canonical cutover.
