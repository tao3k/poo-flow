#+title: Gerbil Bazel Downstream Convergence
#+startup: overview

* Status

Implemented for the shared capability and the POO Flow consumer.  The final
cold-build timing gate remains evidence-driven: it must run only when the
Scheme admission guard reports host pressure below the CPU-adaptive runnable
limit.

* Decision

=gerbil-bazel= is the sole owner of generic Gerbil Bazel capabilities:

- host discovery and Darwin/Linux normalization;
- toolchain and native ABI providers;
- normalized =gxi=, =gxc=, =gxpkg=, and =gxtest= executables;
- =gerbil.pkg=-declared Gerbil package manifest views;
- writable staged project actions driven by the consumer =build.ss=;
- reusable =GerbilProjectInfo=, complete logs, and JSON receipts;
- the workspace-aware =install_dependencies= capability.

Downstream repositories retain only domain rules and build truth.  POO Flow
retains =build.ss=, its Scheme-owned semantic receipt, Scheme project
projection, runtime-C rules, and POO-specific policy.  =gerbil-scheme-rust=
retains Rust/FFI and Cargo domain rules.  Neither downstream owns a second
generic Gerbil repository, toolchain, project compile, project test, or
project-development implementation.

#+begin_src mermaid
flowchart TB
  MODULE["Downstream MODULE.bazel"] --> AUTO["gerbil.auto<br/>platform-adaptive provider"]
  AUTO -->|"Darwin"| HOST["Native Homebrew capability"]
  AUTO -->|"Linux x86_64"| PREBUILT["Immutable prebuilt capability<br/>URL + SHA-256"]
  HOST --> TC["Shared GerbilToolchainInfo"]
  PREBUILT --> TC

  MODULE --> DEP["dependency_gerbil"]
  DEP --> INSTALL["install_dependencies<br/>gxpkg deps --install"]
  INSTALL --> LIB["Workspace .gerbil/lib"]

  MODULE --> PROJECT["local_gerbil"]
  PKG["gerbil.pkg depend:<br/>single package lock"] --> VIEW["project-package-manifest<br/>ready / missing receipt"]
  LIB --> VIEW
  PROJECT --> VIEW
  VIEW --> TC

  SOURCE["Declared downstream sources"] --> STAGE["Writable staged project tree"]
  BUILDSS["Downstream build.ss"] --> ACTION["Shared gerbil_project_compile"]
  TC --> ACTION
  STAGE --> ACTION
  ACTION --> JSONVAL["Scheme exact-one JSON validation"]
  JSONVAL --> INFO["GerbilProjectInfo<br/>project tree + log + JSON receipt"]
  INFO -->|"compiled project first"| TEST["Shared gerbil_project_test matrix<br/>one compile, many tests"]
  TESTROOT["Declared test source-root marker"] -->|"source-only fallback"| TEST
  INFO --> PF["poo-flow<br/>projection + POO/runtime-C rules"]
  INFO --> SR["gerbil-scheme-rust<br/>Rust/FFI + Cargo rules"]
#+end_src

* Public capability contract

Every host or prebuilt provider repository publishes:

- =registered_toolchain= and =toolchain_impl=;
- =native_scheme_env= and the four normalized Gerbil tools;
- =dependency_libraries= and =dependency_library_root=;
- =toolchain.receipt.json=;
- =gerbil_project_compile=, =gerbil_project_dev=,
  =gerbil_project_test=, and =GerbilProjectInfo=;
- =install_dependencies=.

The dependency installer enters =BUILD_WORKSPACE_DIRECTORY=, defaults
=GERBIL_PATH= to the workspace-local =.gerbil= tree, invokes the standard
=gxpkg deps --install= path through the normalized native environment, and
does not duplicate package-manager verification with a second =gxpkg list=
lifecycle.  The provider reads the consumer =gerbil.pkg= =depend:= block as
the package manifest, strips revisions only to locate =.gerbil/lib= package
directories, and records =dependencyPolicy=project-package-manifest= with
per-package =ready= or =missing= state.

* Removed POO Flow legacy surface

The POO Flow consumer no longer carries compatibility implementations for:

- local Gerbil repository discovery;
- a local Gerbil toolchain provider or toolchain type;
- host-system normalization;
- native ABI fingerprinting;
- native tool and environment templates;
- dependency-install templates;
- bootstrap/finalize shell targets and their tests;
- local Scheme project compile/test rules and shell runners.

The shared test API also has no implicit private-compile compatibility path:
every =gerbil_project_test= declaration names one explicit
=GerbilProjectInfo= producer.

Runtime-C contract and test rules consume the same public
=GERBIL_TOOLCHAIN_TYPE= and unified toolchain =runfiles= depset as the Scheme
project rules.  The only remaining local Scheme Bazel rule is the POO-specific
projection from shared =GerbilProjectInfo=.

* Acceptance receipts

** Shared provider

- =gerbil-bazel= main commit: =5b15eea2699aff2c208fa58fe78d0e49bb2040ea=;
- upstream GitHub CI: Linux source and macOS Homebrew-provider jobs passed,
  including ten of ten project, receipt, provider, and declared source-root
  tests;
- platform-adaptive host consumer: passed in 43 seconds;
- synthetic prebuilt consumer: passed in 53 seconds;
- the prebuilt dependency install completed in 3 seconds, re-evaluated the
  project view in 4 seconds, and emitted =dependencyTransition=true=;
- both provider gates resolved the command-name override =GERBIL_CC=cc= before
  toolchain registration and compiled a minimal Gerbil module through the
  selected =gxc -> gsc= chain;
- generic and prefixed receipts are declared action outputs and are validated
  by Gerbil's native JSON reader as exactly one JSON value.

** POO Flow consumer

- =//scheme:deps= resolves to
  =@dependency_gerbil//:install_dependencies=;
- =bootstrap_gerbil= is removed as a repository name; dependency installation
  and project consumption are separate named capabilities;
- both repositories use =gerbil.auto=: Darwin selects the native Homebrew
  provider and Linux selects the immutable prebuilt provider;
- the module extension is =os_dependent=True=, preventing a checked-in
  Darwin evaluation from being replayed as a host provider on Linux;
- the downstream Bazel version is declared as =9.2.0=, matching the shared
  library validation surface;
- the prebuilt installer injects the selected provider environment before the
  first raw =gxpkg= process, so relocated =:gerbil/core= resolution is valid;
- the full Bazel target graph queries successfully after legacy deletion;
- runtime-C resolves through
  =@gerbil_bazel//gerbil:toolchain_type=;
- all six shared Scheme test targets declare =gerbil.pkg= as their source-root
  marker while keeping the compiled project library first on
  =GERBIL_LOADPATH=;
- =//tools/bazel:shared_gerbil_capability_test= passed in 0.4 seconds;
- =//bindings/runtime-c:runtime_v0_symbol_surface_test= passed in 10.5
  seconds.

*** Shared project action convergence

**** Python runtime test environment

The complete Python suite MUST consume a Bazel-declared Gerbil project
environment.  A caller-owned =gxpkg env= invocation is not a maintained
precondition and MUST remain only a runtime fallback for installations that do
not provide the declared environment.

=//scheme:python_runtime_test_environment= consumes =GerbilProjectInfo= from
=//scheme:compile= and the registered Gerbil toolchain.  The launcher resolves
the compiled project root, transitive project dependency roots, the toolchain
library root, =gxi=, and =native_scheme_env= through runfiles.  It therefore
uses the same Darwin and Linux capability selection as the canonical build.

#+begin_src mermaid
flowchart LR
  JUST["just test-python"] --> BAZEL["Bazel project environment target"]
  BAZEL --> PROJECT["//scheme:compile GerbilProjectInfo"]
  BAZEL --> TOOLCHAIN["Registered Gerbil toolchain"]
  PROJECT --> LOADPATH["GERBIL_PATH and ordered GERBIL_LOADPATH"]
  TOOLCHAIN --> LOADPATH
  TOOLCHAIN --> NATIVE["native_scheme_env and toolchain gxi"]
  LOADPATH --> PYTEST["uv pytest complete Python suite"]
  NATIVE --> PYTEST
  PYTEST --> LOADER["Python Scheme loader direct gxi lane"]
  LOADER -. "installation fallback only" .-> GXPKG["gxpkg env gxi"]
#+end_src

The ordered load path is part of the contract:

1. the compiled POO Flow project =.gerbil/lib=;
2. each transitive project dependency =.gerbil/lib=;
3. the toolchain dependency-library root.

The target emits
=poo-flow.gerbil-project-environment-declaration.v1= as a JSON Bazel output.
At execution time it also exports a
=poo-flow.gerbil-project-environment-runtime.v1= JSON receipt together with
the canonical Gerbil project receipt path.  The Python loader consumes the
inherited =GERBIL_PATH=, =GERBIL_LOADPATH=, and toolchain-selected =gxi= path;
it does not reconstruct Bazel output paths.

**** Acceptance receipt (2026-07-22)

- =//tools/bazel:gerbil_project_environment_tool_test=: passed.
- =//tools/bazel:scheme_projection_artifact_tool_test=: passed after sharing
  the same environment composition function.
- Focused Scheme-loader and Funflow projection gate: =6 passed=.
- Maintained =just test-python= gate: =277 passed, 9 skipped, 0 failed= in
  =5.72 s= after the declared project action was cached; Bazel completed the
  cached target action in =0.332 s=.
- An isolated CPython 3.13 environment installed the built wheel and ran
  =python -m poo_flow_benchmarks --list= outside the source tree. The wheel
  contained all =13= benchmark package modules and the module entry point
  listed all =14= registered benchmark cases.
- A clean local target build produced both the executable launcher and
  =python_runtime_test_environment.receipt.json=; the observed full cold target
  build was =328.523 s= and the cached Bazel analysis was =0.187 s=.

- =//scheme:compile=, the development launchers, and the Scheme test matrix
  load only =@gerbil_bazel//gerbil:defs.bzl=;
- the local generic project rule and both local Scheme shell runners are
  deleted;
- all Scheme tests point at one =//scheme:compile= provider instead of
  declaring a compile action per test;
- test-only Scheme modules are declared runfiles and resolve through the
  shared source-root fallback without shadowing the compiled project under
  test;
- the only remaining local Scheme Bazel rule is the POO-specific projection
  from shared =GerbilProjectInfo=.

The local clean POO Flow compile entered the shared Darwin sandbox action but
was correctly stopped by the Scheme admission guard before compilation because
the host reported 92 runnable processes on 12 logical CPUs.  This receipt is
guard evidence, not a cold-time result.  The macOS and Linux GitHub runners own
the unsaturated staged-project acceptance receipt.

* Performance gate

Cold timing is invalid under runnable saturation.  The latest observed host had
12 logical CPUs and 92 runnable processes.  The Scheme guard uses the
machine-adaptive threshold =logical-cpu-count * 2=, emitted
=blocked-host-pressure= with =runnable-saturation=, and reported
=compiled-stage-count=0=.  No downstream migration may bypass that guard or
replace it with a hard-coded CPU count.

The cold acceptance gate is:

1. admission outcome is =ready=;
2. default Bazel sandbox execution completes;
3. the total compile receipt is compared with a receipt carrying the same
   cache state, strategy, CPU count, worker count, and host-pressure fields;
4. a second run proves the incremental cache hit.
