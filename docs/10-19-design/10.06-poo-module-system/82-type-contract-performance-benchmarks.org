* 10.06.82 Contract Migration Performance Evidence
This document preserves historical utilities record-contract measurements and
records qualified native =gerbil-poo= Type/Contract measurements.  The legacy
utilities implementation has been removed; every current workload starts from
native descriptors.

These measurements may locate remaining migration cost, but a fast record or
alist projection cannot establish semantic or performance closure for native
Type descriptors, generic dispatch, contract admission, or evidence reuse.

** 10.06.82.10 Benchmark Boundary
Measure Scheme hot-path work:
- =poo-flow-contract-check-slot!= against reusable native slot schemas;
- native =PooFlowContract. -> alist= projection;
- native =PooFlowContract. -> PooFlowTypeFactContract= projection;
- native =PooFlowContract. -> PooFlowLeanFactContract= projection;
- native session policy / tool-grant descriptor projection and slot admission;
- cached projection reads used by doctor, observability, and runtime-handoff presentation.

Do not measure these as type contract overhead:
- cold =gxi= startup;
- package install or ABI refresh;
- C compiler / Apple SDK failures;
- Lean execution;
- Marlin runtime work;
- external IO, subprocess, sandbox, or connector activity.

#+begin_src mermaid
flowchart LR
  N["native POO Type/Contract"] --> C["element? slot gates"]
  N --> D["bounded alist projection"]
  N --> E["type-fact projection"]
  N --> F["Lean fact contract projection"]
  E --> G["cached proof-facing rows"]
  F --> G
  G --> H["doctor / observability / Marlin handoff"]
  C --> I["POO object construction"]
  I --> H
  N --> O["evidence-bearing Contract admission"]
  N --> P["bounded descriptor projection"]
  O --> I
  P --> H
#+end_src

** 10.06.82.20 Implementation Owners
The benchmark implementation is split by responsibility:
- =t/support/type-contract-performance.ss= owns reusable benchmark workloads.
- =t/type-contract-performance-test.ss= owns regression budgets and receipts.
- =t/performance-tests.ss= includes the suite in the broader performance root.
- =src/testing/project.ss= registers the support file for harness-aware test selection.

The support file is deliberately source-level. It imports the current
=src/module-system/descriptor/contracts.ss=, =src/type-facts=, and
=src/modules/session= owners directly so benchmark results track the active
checkout, not stale installed ABI state.

** 10.06.82.30 Benchmark Classes
Micro benchmark:
- check 10 slots across 2,000 rounds;
- project 10-slot object contracts across 1,500 rounds;
- project type facts and Lean fact contracts across 1,500 rounds.

Scenario benchmark:
- project real native session policy and tool-grant descriptors;
- run real session policy and tool-grant =require-slots!= gates.

Scale benchmark:
- check 100 contract-backed objects with 10 slots;
- check 1,000 contract-backed objects with 10 slots;
- check 10,000 contract-backed objects with 2 slots.

Hot-boundary benchmark:
- project 50 slot facts once;
- read the cached fact rows across 10,000 rounds.

** 10.06.82.40 Historical Regression Budgets
These pre-convergence samples are retained for comparison.  Current gates keep
the same conservative budgets while measuring native descriptors.

| Metric | Current sample | Budget |
| Slot gate, 10 slots x 2,000 rounds | 3.909 ms | 250 ms |
| Object contract alist projection, 10 slots x 1,500 rounds | 8.242 ms | 350 ms |
| Type-fact projection, 10 slots x 1,500 rounds | 35.958 ms | 500 ms |
| Lean fact contract projection, 10 slots x 1,500 rounds | 51.44 ms | 500 ms |
| Session policy/tool-grant scenario, 1,000 rounds | 77.721 ms | 750 ms |
| Scaled family, 10,000 objects x 2 slots | 14.495 ms | 600 ms |
| Cached type-fact reads, 50 slots x 10,000 rounds | 2.092 ms | 200 ms |
| slot-check-ms | 3.934 ms | 250 ms |
| alist-projection-ms | 16.475 ms | 350 ms |
| type-facts-ms | 28.978 ms | 500 ms |
| lean-facts-ms | 31.277 ms | 500 ms |
| session-policy-ms | 45.416 ms | 750 ms |
| scale-10000-ms | 5.012 ms | 600 ms |
| cached-type-facts-ms | 1.324 ms | 200 ms |

These are not final product SLOs. They are baseline regression gates. After CI and local samples stabilize, the budgets should be tightened around observed p50/p95 behavior.

** 10.06.82.45 Native Convergence Receipt, 2026-09-04

The dedicated =//scheme:type_contract_performance_test= passed in 7.0 seconds
after native predicate Types gained their direct =element?= fast path.
The measured Scheme workload excludes Bazel analysis and test-process startup.

| Metric | Workload | Observed | Budget |
|--------+----------+----------+--------|
| Native session policy/tool-grant projection and admission | 1,000 rounds, best of 3 | 14.490 ms | 750 ms |
| Native slot gate | 10 slots x 2,000 rounds, best of 3 | 4.156 ms | 250 ms |
| Native Contract alist projection | 10 slots x 1,500 rounds, best of 3 | 8.835 ms | 350 ms |
| Native type-fact projection | 10 slots x 1,500 rounds, best of 3 | 43.099 ms | 500 ms |
| Native Lean-fact projection | 10 slots x 1,500 rounds, best of 3 | 40.594 ms | 500 ms |
| Scaled native family | 10,000 objects x 2 slots, best of 3 | 5.104 ms | 600 ms |
| Cached type-fact reads | 50 slots x 10,000 rounds, best of 3 | 1.419 ms | 200 ms |

All current metrics measure native descriptor projection and Type-backed slot
admission.  Closed predicate Types use direct =element?= and =validate= slots;
explicit classification and Contract admission still produce evidence.

** 10.06.82.46 Shared Schema and Loop Build Receipt, 2026-09-03

The graph, sandbox, session, and performance gate run completed in 611.626
seconds with a 576.14-second critical path.  Its test actions passed in 11.0,
10.0, 4.0, and 18.7 seconds respectively.  The immediately repeated warm run
completed in 0.555 seconds with all four actions served from cache.

After the loop governor and human-audit migration, the loop, unit, and contract
gate run completed in 532.198 seconds with a 494.08-second critical path.  The
three test actions passed in 16.1, 186.6, and 18.1 seconds respectively.  The
immediately repeated warm run completed in 0.840 seconds with all three actions
served from cache and no project compilation.

These cold totals measure package-level invalidation and test aggregation, not
native Type dispatch.  The warm receipts establish deterministic cache reuse;
the multi-minute cold critical paths remain a Building Framework granularity
optimization target.

After consolidating both former loop utilities scenarios into the native loop
gate, its final cold run completed in 300.747 seconds with a 299.70-second
critical path; the test action itself passed in 14.4 seconds.  The immediate
warm rerun completed in 0.196 seconds with one action-cache hit and no project
compilation.

** 10.06.82.47 Final Native Convergence Gate, 2026-09-04

The final gate covered observability, module objects, contract schema, JSON
Schema, type-fact projection, performance, graph, sandbox resource, session,
loop, contract aggregation, and unit tests.  All 12 explicit owner targets
passed.  The first combined invocation completed in 107.171 seconds with a
32.39-second critical path.  Two prior results were cached and 10 tests ran;
there was no multi-minute Scheme or GCC compilation action.

The immediately repeated identical invocation completed in 1.544 seconds with
a 0.01-second critical path.  Bazel reported 12 action-cache hits, executed
zero tests, and reused all 12 passing test results.  This is the stable warm
receipt for the completed native convergence slice.  Package-level cold
rebuilds remain a separate Building Framework action-granularity concern.

** 10.06.82.48 Sandbox Profile Contract Receipt, 2026-09-04

The first owner-gate invocation after a Bazel daemon restart completed in
1,018.271 seconds with a 989.38-second critical path.  The dependency chain
materialized =gerbil_utils=, =gerbil_poo=, =asp_gerbil_scheme=, and the POO Flow
package before the 44.4-second test action.  The immediately repeated identical
owner gate completed in 1.206 seconds with one action-cache hit and zero tests
executed.  This separates the package-level cold path from native profile
Contract admission.

The integration and unit aggregates passed in 234.032 seconds with their
compile action cached.  A separate sandbox profile/resource/UI boundary run
found a false-positive portability fixture: its complete recipe occupied the
macro's profile-name position, so none of its intended rows were tested.  After
correcting the declarative form, the native Contract and UI gates both passed
in 37.240 seconds.  The Contract remains strict; the test data was corrected
instead of weakening profile-name or slot Types.

The final five-target boundary run covered native profile and resource
Contracts, sandbox UI, integration, and unit tests.  All five passed in 157.883
seconds with a 57.78-second critical path and no package compilation.  Its
immediately repeated identical invocation completed in 1.351 seconds with five
action-cache hits and zero tests executed.

After routing derived-parent validation through the same native Contract, the
final source-changing owner/integration run completed in 559.975 seconds with a
518.95-second critical path.  Only the POO Flow package rebuilt; the three
dependency packages remained cached.  The owner and integration tests passed
in 37.9 and 155.5 seconds.  The immediately repeated identical invocation
completed in 0.760 seconds with two action-cache hits and zero tests executed.

The remaining cold-build hotspot is therefore POO Flow package action
granularity: a single Scheme owner change invalidates the whole project compile
action.  Before input isolation, every Scheme test also declared
=docs/**/*.org= as an input, so receipt-only documentation edits invalidated
test results even though they did not rebuild the package.  Both are build-graph
concerns, not reasons to weaken native Contract admission.

** 10.06.82.49 Documentation Input Isolation, 2026-09-04

No Scheme test reads =README.org= or any file under =docs/=.  Documentation
paths present in tests are expected command strings, not runtime file inputs.
POO Flow therefore removed those globs from the shared
=scheme_test_sources= filegroup while retaining actual test sources, the
compiled project, and the explicit JSON Schema fixture.

The first owner/integration run after changing the Bazel declaration completed
in 63.173 seconds; both tests passed and no project package compiled.  The
immediately repeated baseline completed in 1.588 seconds with two action-cache
hits and zero tests executed.  After this Org-only receipt update, the identical
invocation completed in 1.298 seconds with the same two action-cache hits and
zero tests executed.  Documentation is therefore absent from both the project
compile key and the representative Scheme test keys.

** 10.06.82.50 Native Test Source Isolation, 2026-09-04

The 11 independent Native Type/Contract and focused performance targets now
declare precise test inputs through the upstream =gerbil_project_test.srcs=
interface.  Ordinary owner tests receive only the compiled Scheme project;
the JSON Schema target additionally owns its three imported test modules and
schema fixture, while the Type/Contract performance target owns its two support
modules.  Aggregate unit, integration, contract, CLI, and performance suites
retain the broad =t/**= input because their roots intentionally import many test
modules.

All 11 precise targets passed after the declaration change in 104.104 seconds
without package compilation.  Their immediately repeated invocation completed
in 1.317 seconds with 11 action-cache hits and zero tests executed.

For the isolation acceptance check, one self-contained sandbox-profile test
source changed after the warm baseline.  The sandbox-profile target reran and
passed, while the graph target remained cached.  The two-target invocation
completed in 23.665 seconds with exactly one action-cache hit and one test
execution.  A single owner-test edit therefore no longer invalidates unrelated
Native owner gates.  After recording this receipt in Org, the same two targets
completed in 0.933 seconds with two action-cache hits and zero tests executed.

** 10.06.82.51 ASP Policy Scan Concurrency Receipt, 2026-09-04

POO Flow continues to declare the complete =src + t + user-interface= source
universe to the ASP Build API.  Coverage was not reduced.  One policy
invocation parses every selected owner once into a shared =ProjectIndex=, then
all rule families reuse those parser-owned facts.

#+begin_src mermaid
flowchart LR
  Build["POO Flow build.ss\n805 declared Scheme owners"]
  Pool["ASP parser pool\nmin(files, available CPUs)"]
  Index["one shared ProjectIndex"]
  Policy["all policy families"]

  Build --> Pool --> Index --> Policy
#+end_src

The ASP upstream parser still had a fixed default ceiling of four workers even
on a 12-core host.  A controlled POO Flow full-coverage A/B used the existing
override without changing any source set:

| ASP parser configuration | policy-collect | policy-checks | policy-json | Total measured phases |
|--------------------------+----------------+---------------+-------------+-----------------------|
| Fixed default, 4 workers | 11.202079 s | 6.981182 s | 0.202017 s | 18.385278 s |
| Explicit 12-worker A/B | 7.716923 s | 4.221497 s | 0.076965 s | 12.015385 s |
| Patched available-core default, no override | 8.605124 s | 5.009181 s | 0.057033 s | 13.671338 s |

The controlled A/B reduced parser collection by 31.1 percent and the three
measured policy phases by 34.6 percent.  The no-override validation reported a
default worker count of 12 and reduced the same phase total by 25.6 percent
relative to the old default.  Host load remained elevated, so these samples
establish direction and interface correctness rather than a stable p50.

The ASP regression captures native parser trace rows during one
=policy-source-report= and requires exactly one start event for each owner.  Its
focused suite passed 98 checks under the existing 512 MiB managed-heap profile.
Memory protection remains anomaly detection; it no longer pre-emptively hides
available parser parallelism with a fixed worker ceiling.

The separate POO Flow native =std/make= warm receipt completed in 61.263 seconds
with zero native compiler jobs while host load average remained approximately
59--64.  That result concerns upstream dependency/frontend checking and is not
attributed to the ASP parser phase.

** 10.06.82.55 Verification Commands
Use the project Bazel gate; the target runs the Scheme hot path through the
same declared project environment as other tests:

#+begin_src shell
.devenv/devenv-profile-exec bazelisk test //scheme:type_contract_performance_test
#+end_src

Native source expansion remains a separate semantic gate and is not included in
the performance receipt.

** 10.06.82.60 Design Rule
Type contracts may be strict at declaration, construction, doctor, and handoff boundaries.

Agent-loop hot paths should use cached immutable contract projections unless the object graph changed. Repeated full type-fact or Lean-fact projection inside every loop step is a performance bug unless a specific module proves the graph changed and the projection must be recomputed.
