#+title: AC-11 Canonical Cutover Ledger
#+subtitle: Release decision boundary, canonical routes, retirement candidates, and deletion gates
#+author: POO Flow Contributors
#+date: 2026-07-12
#+status: Inventory ready; canonical cutover blocked on explicit migration approval
#+options: toc:3 num:t

* Decision boundary

This ledger is the non-destructive entry artifact for AC-11.  AC-10 has closed
the implementation and integrated qualification prerequisites, but RFC 94 P6
also requires an accepted release decision identifying the canonical routes.
That release decision has not yet been recorded.

Therefore this document:

- records the routes that are already qualified to become canonical;
- records retirement candidates and the evidence required before deletion;
- does not freeze ABI v1;
- does not remove shadow code, tests, public exports, or package surfaces;
- does not grant compatibility aliases or a second semantic route.

Explicit migration approval MUST name this ledger (or a successor revision) as
the accepted canonical-route set before any row may move to =remove= or
=publish/freeze=.

#+begin_src mermaid
flowchart LR
    Q["AC-10 qualified release"]
    L["AC-11 cutover ledger"]
    D{"Explicit release decision?"}
    A["Absence and downstream gates"]
    R["Single canonical route"]
    B["Inventory only; no deletion"]

    Q --> L --> D
    D -->|"no"| B
    D -->|"yes"| A --> R
#+end_src

* Status vocabulary

| Status | Meaning |
|--------+---------|
| =retain= | Already-qualified canonical owner or validation route |
| =relabel= | Retain implementation, update public naming/status at cutover |
| =audit= | Exact public owner and consumers must be resolved before a decision |
| =remove-after-approval= | Bounded retirement candidate; deletion is P6 work |
| =publish-after-approval= | Qualified artifact that becomes a release surface only through the release decision |
| =absence-guarded= | No implementation owner exists and a bounded regression gate preserves that fact |
| =blocked= | No mutating action is authorized yet |

* Canonical route ledger

| Capability | Canonical owner and public route | Validation or publication evidence | Status |
|------------+----------------------------------+------------------------------------+--------|
| Bundle authoring and identity | POO-native =src/semantic/organization-bundle.ss= through its contract facade | Bundle, Kernel, five-facet, mutation, and canonical fixture scenarios | =retain= |
| Runtime control projection | =src/contract/organization-bundle-runtime-v0.ss= | Scheme projection plus strict and installed C consumers | =retain= |
| Runtime hot and bulk projection | =src/contract/organization-bundle-runtime-v0-batch.ss= and =src/contract/runtime-v0-abi-schema.ss= | 140-block matrix, sanitizer, leak, parity, and installed-consumer gates | =retain= |
| Native runtime boundary | =bindings/runtime-c/include/poo_flow/runtime_v0.h= and =bindings/runtime-c/src/runtime_v0.c= | Bazel C suites and generated-contract parity | =relabel= |
| Python production runtime | Out-of-line CFFI API mode under =packages/python-runtime/src/poo_flow_runtime/_native/= | Isolated wheel without Gerbil and Python production/conformance suites | =retain= |
| Authorization and evidence | AuthorizedEffectToken, native evidence sink, and evidence-chain contract owners | Allow, deny, replay, revocation, substitution, fork, and truncation gates | =retain= |
| Proof projection and theorem identity | =src/proof/proof-case-projection.ss=, generated ProofCaseVector v1, and locked Lean package | Four-way differential, Lean build, FFI smoke, mutation, and stale-proof rejection | =retain= |
| Release assurance | =src/contract/release-assurance-manifest.ss=, claim verifier, and =src/qualification/release-assurance.ss= | AC-10 twelve-gate revision-bound manifest | =publish-after-approval= |

* Retirement ledger

| Capability being retired | Retirement owner or candidate | Replacement | Required deletion evidence | Status |
|--------------------------+-------------------------------+-------------+----------------------------+--------|
| AC-04 semantic shadow comparison | =src/semantic/organization-bundle-shadow.ss=, its contract facade, and =t/scenarios/organization-bundle-shadow-test.ss= | Canonical Bundle plus AC-10 cross-boundary fixture | No public export or consumer; shadow owner/test absent; qualification still passes | =remove-after-approval= |
| Agent-sandbox alist authoring helpers | Four exports in =src/modules/agent-sandbox/alist.ss=; zero parser-visible consumers or facade re-exports | Named POO-native helpers in =src/modules/sandbox-core/profile-support/authoring.ss= | Fresh consumer audit; remove module/exports; old-symbol absence gate; sandbox profile suites | =remove-after-approval= |
| Other raw-record, alist serialization, and =.def= occurrences | No competing public authoring owner proven | Retain internal representations and POO implementation syntax | A future candidate requires exact public export and consumer evidence | =retain= |
| Python =ctypes= production hot path | No parser-visible import/call/test owner under =packages/python-runtime= | Out-of-line CFFI API mode | AST import/dynamic-import guard over =src/= and =tests/=; README absence; isolated wheel and full Python gates | =absence-guarded= |
| Runtime graph or ABI compatibility aliases | No parser-visible owner, consumer, wrapper, or installed graph symbol | Installed =runtime_v0.h= and ProofCaseVector v1 routes | Shared-library exported-symbol allowlist plus graph/compat/legacy denylist and installed consumers | =absence-guarded= |
| Draft labels on qualified Bundle/shadow artifacts | Schema and artifact owners named by each row | Explicit release versions chosen by the release decision | Version matrix, regeneration parity, migration/rollback statement | =blocked= |
| Shadow dual-read or dual-projection documentation | RFC 94, plan 95, matrix 96, AC-04 design 110 | Canonical-route documentation | Documentation link/term audit and no retired examples | =remove-after-approval= |

Internal =object<-alist= construction, alist serialization at explicit contract
boundaries, and POO implementation use of =.def= are not retired merely by name.
Only a public authoring route that competes with the accepted POO-native surface
is eligible for removal.  The exact-owner audit must distinguish those cases.

** Authoring audit receipt

The parser-owned audit resolves exactly four competitive public alist exports,
all owned by =src/modules/agent-sandbox/alist.ss= and currently reporting zero
parser-visible consumers and zero facade re-exports:

| Retired export candidate | Canonical POO-native replacement |
|--------------------------+----------------------------------|
| =agent-sandbox-option= | =poo-flow-sandbox-profile-object-row-slot= and =poo-flow-sandbox-profile-object-row-value/default= |
| =agent-sandbox-alist-ref= | =poo-flow-sandbox-profile-object-row-value= and =poo-flow-sandbox-profile-object-row-field= |
| =agent-sandbox-merge-alists= | =poo-flow-sandbox-profile-object-row-merge= and =poo-flow-sandbox-profile-object-field-with-merge= |
| =agent-sandbox-override= | =poo-flow-sandbox-profile-object-row-contribution= and =poo-flow-sandbox-profile-object-row-operator= |

These counts are dependency-topology evidence, not permission to delete.  The
source index freshness gap must be repaired or compensated by the bounded
old-symbol absence gate in the approved removal commit.

The same audit explicitly retains:

- =object<-alist= in durable-artifact decoding as bounded internal conversion;
- =defstruct= values in =src/contract/json-schema-ir.ss= as internal schema IR;
- =.def= used to define POO prototypes rather than a competing public DSL.

** Python no-ctypes receipt

The Python parser resolves CFFI as the package dependency owner and finds no
=ctypes=, =CDLL=, or =PyDLL= owner.  The existing public-surface contract test
now enforces this boundary with an AST-based import and dynamic-import scan over
=packages/python-runtime/src= and =packages/python-runtime/tests=, plus an
explicit README absence assertion.  This is a regression guard, not a deletion
claim.

* Package and publication ledger

| Surface | Cutover action | Gate | Current state |
|---------+----------------+------+---------------|
| =gerbil.pkg= and public Scheme facades | Publish only the canonical semantic and contract closure | Clean release build plus public export exact-set check | =audit= |
| Python =pyproject.toml=, wheel, and bundled native library | Record supported Bundle/ABI versions and CFFI ownership | Isolated wheel, no-Gerbil runtime, and native negotiation gates | =publish-after-approval= |
| Runtime C headers and libraries | Publish explicit pre-v1 or approved v1 identity; never imply freeze | Installed C consumer, sanitizer, leak, performance, symbol set | =publish-after-approval= |
| Proof vector, Lean environment, and proof package metadata | Publish exact schema, theorem-set, and import-environment identities | Generated freshness, Lean build, Python/C/Scheme differential | =publish-after-approval= |
| Benchmark and qualification receipts | Bind source revision, environment, schemas, and declared unsupported claims | AC-10 manifest verification | =publish-after-approval= |

* Release version matrix

The executable owner is =src/qualification/release-version-matrix.ss=.  It
binds the qualified artifacts without promoting or renaming them:

| Dimension | Current bound value | Normative owner | Freeze state |
|-----------+---------------------+-----------------+--------------|
| Bundle schema | =poo-flow.organization-bundle.draft.3= | =src/semantic/organization-bundle.ss= | pre-release |
| Runtime ABI | major =0=, minor =1= | =src/contract/runtime-v0-abi-schema.ss= and generated C contract | =abi-v1-frozen? #f= |
| Proof vector | version =1= plus schema fingerprint and digest domains | generated ProofCaseVector v1 and TOML source | qualified v1 artifact |
| Evidence/assurance | =poo-flow.release-assurance-manifest.v1= | assurance manifest, verifier, and qualification owners | qualified v1 artifact |

The verifier fails closed on owner/version drift and on any implicit ABI v1
freeze.  A release decision remains required even when this matrix verifies.
The machine-readable current receipt is emitted with:

#+begin_src sh
gxi tools/run-ac11-release-version-matrix.ss
#+end_src

The runtime shared-library gate owns the exact exported-symbol allowlist beside
the installed C consumers.  It rejects any unreviewed export and explicitly
denies graph, compatibility, or legacy-named public symbols.

* Approval record required for mutation

The P6 release decision must record all of the following in one reviewable
receipt:

1. accepted revision of this ledger;
2. chosen Bundle schema and runtime ABI versions;
3. canonical public route for every retained capability;
4. every row authorized for deletion;
5. rollback as a new validated epoch, never revival of retired semantics;
6. required downstream installed-consumer and absence gates;
7. source revision and AC-10 assurance-manifest identity.

Until that receipt exists, AC-11 state remains =InventoryReadyApprovalBlocked=.

* Execution order after approval

#+begin_src mermaid
flowchart TD
    E["Resolve exact public owners"]
    V["Choose explicit schema and ABI versions"]
    C["Cut canonical docs, facades, and package metadata"]
    X["Delete shadow and retired routes"]
    T["Run absence, conformance, fault, and performance gates"]
    P["Publish revision-bound release receipt"]

    E --> V --> C --> X --> T --> P
#+end_src

Removal is a dedicated AC-11 feature tranche and MUST NOT be mixed with new
semantic construction.  A failed absence or downstream gate restores the
branch to the pre-cutover revision; it does not add a compatibility wrapper.

* Immediate next gate

The initial authoring and Python =ctypes= audits are closed.  Before requesting
the release decision:

- wait for or repair the external ASP provider/state-schema migration so exact
  owner selectors can be independently re-certified; this is not a POO Flow
  product-source defect;
- bind the four old-symbol absence checks to the dedicated post-approval
  removal tranche;
- bind the approved deletion set and chosen release versions into the P6
  release decision.

No candidate is removed as part of this audit.

* Normative references

- [[file:94-agentic-control-plane-migration-rfc.org][RFC 94 — P6 Canonical Cutover And Removal]]
- [[file:95-agentic-control-plane-execution-plan.org][Execution Plan — AC-11]]
- [[file:96-agentic-control-plane-owner-test-matrix.org][Owner And Test Matrix]]
- [[file:110-agentic-control-plane-ac04-shadow-projection-design.org][AC-04 Shadow Projection Design]]
- [[file:132-agentic-control-plane-ac10-qualification-receipt.org][AC-10 Qualification Receipt]]
